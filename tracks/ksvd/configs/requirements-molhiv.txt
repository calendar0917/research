# molhiv phase (P0+)
numpy>=1.24
scikit-learn>=1.3
torch>=2.0
torch-geometric>=2.4
ogb>=1.3.6
pandas>=2.0
tqdm>=4.65
# optional for GINE dual-channel
# torch-scatter / torch-sparse: install matching torch+cuda from pyg.org if needed
