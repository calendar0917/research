# Legacy compatibility file. Do not install this file directly.
# The reproducible KSVD environment is locked at the repository root:
#
#   uv sync --frozen
#
# Add or update dependencies only in ../../../pyproject.toml, then regenerate
# ../../../uv.lock with `uv lock`.
