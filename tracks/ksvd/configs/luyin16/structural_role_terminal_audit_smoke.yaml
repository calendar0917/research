protocol_id: luyin16-molhiv-rooted-wl-terminal-audit-v1-smoke

data:
  dataset: ogbg-molhiv
  root: data/ogb
  scaffold_folds: tracks/ksvd/results/molhiv/molhiv_n41127_scaffold_folds3_seed20260726.npz
  warm_start_result: tracks/ksvd/results/luyin16/task_aligned_interaction_screen/summary.json
  dev_feature_cache: /tmp/structural_role_terminal_audit_smoke_dev.npz
  test_feature_cache: /tmp/structural_role_terminal_audit_smoke_test.npz

representation:
  radius: 2
  degree_bins: 8
  wl_rounds: 2
  node_role_bins: 64
  edge_role_bins: 32
  typed_edge_bins: 256
  max_centers_per_graph: 8

tuning:
  n_trials: 2
  seed: 20260907
  n_jobs: -1
  ranges:
    n_estimators: [20, 60]
    max_depth: [2, 4]
    learning_rate: [0.04, 0.16]
    min_child_weight: [1.0, 12.0]
    subsample: [0.70, 1.0]
    colsample_bytree: [0.40, 1.0]
    reg_lambda: [0.5, 30.0]
    reg_alpha: [0.0001, 2.0]
    gamma: [0.0, 2.0]
  view_static:
    t_a:
      max_bin: 64
    f_centered:
      max_bin: 32

fixed_classifier:
  n_estimators: 30
  max_depth: 3
  learning_rate: 0.08
  min_child_weight: 4
  subsample: 0.85
  colsample_bytree: 0.80
  reg_lambda: 10.0
  reg_alpha: 0.01
  gamma: 0.0
  n_jobs: -1

model_seeds: [0]

search_checkpoint_json: /tmp/structural_role_terminal_audit_smoke_checkpoint.json
frozen_manifest_json: /tmp/structural_role_terminal_audit_smoke_manifest.json
frozen_manifest_markdown: /tmp/structural_role_terminal_audit_smoke_manifest.md
terminal_test_json: /tmp/structural_role_terminal_audit_smoke_test.json
terminal_test_markdown: /tmp/structural_role_terminal_audit_smoke_test.md
