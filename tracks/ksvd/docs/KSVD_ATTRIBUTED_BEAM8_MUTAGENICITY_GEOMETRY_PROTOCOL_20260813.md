# Attributed Beam8/Mutagenicity geometry protocol

> 日期：2026-08-13  
> 状态：attributed invariance 通过后冻结；不使用 graph labels。

严格复用旧 typed feasibility 的指标与 gate，只将表示改为已通过 100% 重编号不变量的
node/edge-attributed Beam8：nauty attributed whole-graph coordinates + rooted typed canonical
slots。比较 `s10/o3` 与 `s8/o2` 的 chain、coverage、逐 bond recall，以及 node/new-bond
TRUE−SHUFFLED margin。旧 binary-order audit 不参与新 geometry 选择。

