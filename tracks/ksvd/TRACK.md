# Track: KSVD 结构字典学习

> **2026-09-07 起**：本 TRACK 不再承担实验流水数据库职责。新实验统一走
> `uv run research run` → `runs/`（本地执行真相，Git-ignored）；有意义 →
> `uv run research promote` → `records/runs/`（durable facts，Git-tracked，
> fresh clone 可读）；判断/决策进 `records/{claims,decisions}`；
> 导航见 `STATE.yaml`。`results/luyin16/` 保留为 legacy evidence archive
> （只读，不再作为新实验数字的默认入口）。下方历史进度保留，不代表当前记录方式。

| 项 | 内容 |
|----|------|
| slug | `ksvd` |
| 状态 | **active** |
| 创建 | 2026-07-23 |
| 主线位置 | KSVD（相对 Kernel / GSN） |
| 定义笔记 | [notes/definition.md](notes/definition.md) |
| 主录音 | `docs/luyin/luyin10.txt`（+ luyin3/4 机制） |

## 问题（一句话）

如何用 **数据驱动的稀疏字典（KSVD）** 把图局部结构变成 **可还原** 的显式表征，并在感受野受限时（探索：随机游走构子图）仍保持可辩护的结构意义与下游可用性。

## 范围

- **做**
  1. 组内 KSVD 定义与 I/O（\(Y,D,X\)、readout）— 见 `notes/definition.md`
  2. 一阶邻域基线的缺陷与理论边界（>1-WL、感受野上限）
  3. 随机游走相关文献调研 → 约束下扩大感受野 / 降边冗余的方案草稿
  4. 最小实验设计（协议 lock 后）
- **不做**
  - 重开 GNN 全库复现（`gnn-gsn` archived）
  - 未调研前直接实现「无约束 RW 删边」当最终方法
  - 跨轨混协议主表

## 协议 / 评估

- 登记：[notes/protocol.md](notes/protocol.md)
- 主下游：`ogb-molhiv-v0` + PyG `ZINC subset=True`（official split，luyin16 已跑通）；过程：`stage0-sample-v0`
- TUD/CIN 乐观 10-fold：**只引用**，不进 strict 主表
- 目的分维：A / B **分表**

## 进度

| 阶段 | 状态 |
|------|------|
| 问题与范围 | **done** |
| 精读 / RW 调研 / CIN 协议 | **done** |
| 执行计划 | [notes/plan.md](notes/plan.md) |
| **阶段 0–3 烟测** | **done** → `results/SUMMARY.md` · `results/full_pipeline.json` |
| molhiv 主对标 | **done**（luyin16 官方 scaffold：显式统计 S=0.7817，分布读出 0.8341 valid，严格 test 0.8087 / 集成 0.8135） |
| 池化 / 融合阶段 | **已成判断**：机制冻结在统计交互 + XGBoost；可学习融合（attention/FiLM/gate）无稳定增量 |
| 交付 | 总览已写：`results/luyin16/EXPERIMENT_ROUTE_SUMMARY_20260829_ONWARD.md`；正式论文表未做 |

### luyin16 阶段入口

- 阶段说明：`notes/luyin16_plan.md`
- 阶段总览：`results/luyin16/EXPERIMENT_ROUTE_SUMMARY_20260829_ONWARD.md`
- 配置目录：`configs/luyin16/`
- 结果目录：`results/luyin16/`
- 新实验入口：`experiments/luyin16/`
- 可复用包：`src/ksvd_research/`；新代码不要依赖顶层 `code` 名称
- 原则：先验证结构特征的独立价值，再决定是否进入结构—语义融合；不继续无目的堆叠模型。

## 入口

| 路径 | 用途 |
|------|------|
| `src/ksvd_research/` | 可复用核心包（当前为兼容层，逐步迁移） |
| `experiments/luyin16/` | luyin16 新实验入口 |
| `code/` | 历史实现与复现入口，不再平铺新增实验 |
| `tests/` | pytest 维护测试 |
| `configs/` | 配置 |
| `runs/` | 本地执行真相（Git-ignored，控制面自动写） |
| `records/runs/` | durable promoted facts（Git-tracked） |
| `records/claims/` `records/decisions/` | 科研判断 / 路线决策 |
| `results/luyin16/` | legacy evidence archive（2026-09-07 前后历史体系，只读） |
| `notes/` | 决策与定义 |
| `docs/` | 短说明 |

## 禁止

- 与 gnn-gsn 结果混表横比（除非单独 deliverable 写清协议）
- 在 tracks/ 之外另立仓库扩张本课题（旧仓 `../../paper` 已不可访问）
- 把表达力证明与 TUD/OGB Acc 混为一谈

## 下一步

1. ~~写清组内 KSVD 定义~~ → `notes/definition.md`
2. ~~RW 文献调研 v0.1~~ → `notes/rw_survey.md`（主候选 **R2+RW-C0**）
3. ~~精读 P0~~；~~评估+分阶段实验草案~~ → `notes/rw_survey.md` §7–§8
4. **已定**：向量化 = 诱导邻接 pad→\(m\)；下游对标 **CIN / molhiv 方向**（见 [cin.md](../../docs/literature/deep/cin.md)）
5. ~~烟测 / followup / fixes~~ → `results/FIXES_SUMMARY.md`：**共享字典**在合成 0.55→0.86
6. ~~rich 读出 / distant / MUTAG gate~~ → `results/NEXT_SUMMARY.md`（distant 被局部度破解，RW 反降分）
7. ~~C4 vs C8~~ → `results/C4_SUMMARY.md`：**B0~0.85，RW+共享KSVD~1.0**
8. ~~可视化 + GIN paper-optimistic 拼接~~ → `results/viz/` · `GIN_STRUCT_SUMMARY.md`（拼接 **降分**）
9. 共享字典说明：`notes/shared_dict.md`（非一开始就定，fixes 后采纳）
10. ~~节点级系数融合~~ → `NODE_STRUCT_SUMMARY.md`（节点级 93.6% ≫ 广播 83.5%；≈ gin_only）
11. ~~Xu 全局 epoch 协议对齐~~ → `GIN_XU_PROTOCOL_SUMMARY.md`（gin_only **89.4%** 贴论文）
12. ~~融合对照（concat/residual/gate）~~ → `FUSION_XU_SUMMARY.md`（concat 90.4 微升；残差/门控≈only）
13. ~~RW 融合~~ → `FUSION_XU_RW_SUMMARY.md`（gate 90.9；concat/residual 变差）；`notes/rw_params.md`
14. ~~多 walk（r=5）+ pool~~ → `FUSION_XU_RW_R5_SUMMARY.md`（concat 回升但仍 ≤ only）
15. ~~文献 v0.2 + 精读补全 + 调研报告~~ → `notes/rw_survey.md` · `docs/survey_report.md`
16. ~~RW 可行性+CIN路线~~ → `notes/rw_feasibility_and_cin_path.md`
17. ~~过程指标 R1/R4/R5/R6~~ → `results/COVERAGE_RF_SUMMARY.md` · `code/coverage_sample.py`
18. ~~图级取样做实~~ → `GRAPH_LEVEL_SOLID_SUMMARY.md` · `notes/graph_level_algorithm.md`
19. ~~交互可视化 HTML~~ → `results/viz_pipeline/index.html`
20. 与导师对报告；分子下游后置
21. ~~molhiv 阶段计划 + 协议细则~~ → `notes/molhiv_phase.md` · `protocol.md`
22. ~~MIL attention pool + 合成消融脚本~~ → `ksvd.pool_X` · `run_pool_ablation.py`
23. ~~结构-only / 双通道骨架~~ → `run_molhiv_probe.py` · `run_molhiv_dual.py`
24. ~~装 ogb + 探针~~ → `results/molhiv/MOLHIV_PHASE_SUMMARY.md`
25. **发现**：n8k 结构-only max test≈0.61 > degree；n3k dual concat **未超** gine_only
26. ~~设计消融 n6k~~ → `results/molhiv/DESIGN_VERDICT.md`
27. **有效**：coverage + max + 小字典 A8T2；残差相对 size +3～4pt valid
28. **无效**：抬 cover、mean、图级 concat；下一步节点级融合 / patch 语义
29. ~~B/C/D/A next-round~~ → `results/molhiv/NEXT_ROUND_VERDICT.md`
30. **下调**：residual 多种子不稳；chem/ring/node_gate 均无稳定超 GINE
31. **收窄主线**：采样+可还原字典机制；或环/cell 对象升级；molhiv 增益暂非主证据
32. ~~luyin14 路线闭环~~ → `results/luyin14/ROUTE_CLOSURE_20260812.md`
33. **补边 no-go**：EDGE100−FAIR95 在四数据集均值全负；结合既有 rate 审计，默认 FAIR95 + residual sidecar
34. **关系 no-go**：TRUE−SHUFFLED 仅 IMDB-BINARY +1.1pt，未跨数据集复现
35. **融合 no-go**：MUTAG/PTC_MR 的 KSVD 融合均未超 feature-only；简单 feature+stats 反而稳定更强
36. **路线结论**：普通 KSVD 固定为 compressor / diagnostic baseline；不再扫描 K/T、restart、fusion depth
37. ~~rich sparse-code readout 迁移审计~~ → `results/luyin14/RICH_READOUT_20260812.md`
38. **rich 归因结论**：相对 coarse 在两个 IMDB 上 `+3.9/+2.4pt`、均 9/9 wins；但 FINAL−INIT 仅 `+0.4/+0.3pt`，且没有 graph-stats 外增量
39. ~~relation-conditioned structured pursuit~~ → `results/luyin14/STRUCTURED_PURSUIT_20260812.md`
40. **structured no-go**：support agreement 明显上升，但 reconstruction 恶化 `+33%` 到 `+271%`，三个主 gate 全失败
41. ~~uncompressed RAW relation terminal screen~~ → `results/luyin14/RAW_RELATION_20260812.md`
42. **RAW relation 结论**：TRUE 相对弱 bag 在 IMDB-BINARY/MUTAG 有增益，但不稳定胜 SHUFFLED、四数据集均无 stats 外增量；停止同一 TUD 上的 patch-graph/Transformer
43. **下一研究问题**：若继续 downstream，改做低阶统计匹配、标签由 patch 空间组合决定的 benchmark；先跑 RAW/INIT/FINAL × BAG/TRUE/SHUFFLED，再决定是否需要 relation-aware encoder
44. 完整决策：`results/luyin14/NEXT_DIRECTION_DECISION_20260812.md`
45. ~~节点级 strict concat/FiLM~~ → `results/luyin14/NODE_LEVEL_FUSION_STAGE_A_20260812.md`：MUTAG 有位置绑定信号，但 FINAL=INIT；PTC_MR 不复现
46. ~~OOF late fusion / patch-local bilinear~~ → `results/luyin14/OOF_MULTIVIEW_FUSION_20260812.md`、`PATCH_LOCAL_MULTIMODAL_20260812.md`：融合容量不是主瓶颈
47. ~~structure–attribute shared-code dictionary~~ → `results/luyin14/JOINT_MULTIVIEW_DICTIONARY_STAGE_A_20260812.md`：解决无类型 patch 的零学习余量，但跨数据集 gate 失败
48. ~~edge-aware joint dictionary + GINE，3 split seeds~~ → `results/luyin14/EDGE_AWARE_JOINT_MULTI_SPLIT_20260812.md`
49. **edge-aware 结论**：seed-0 MUTAG 强正，但 9-fold 汇总仅 `+4.3pt`、5/9 wins，binding 3/9 wins；PTC_MR `-2.1pt`，不稳定
50. **下一优先**：换更大、带 node/edge attributes 的真实 benchmark 做固定协议外部验证；先升级 typed radius-2 patch 语义，不增加 cross-attention
51. ~~TU 中等规模外部验证~~ → `results/luyin14/TUD_EXTERNAL_VALIDATION_20260813.md`
52. **Mutagenicity Stage A**：seed 0 FINAL−GINE `+7.0pt`、TRUE−SHUFFLED `+3.3pt`、FINAL−INIT `+3.9pt`，按协议扩展
53. **Mutagenicity 9-fold 结论**：FINAL−GINE `-0.4pt`（3/9 wins）；binding `+0.8pt`（5/9），FINAL−INIT `+0.6pt`（5/9），强信号不跨 split
54. **NCI1 结论**：FINAL−GIN `+4.4pt`，但 FINAL−INIT `-1.3pt`；shared prototype 有用，不能归因于 K-SVD updates
55. **radius-2 substrate PASS**：两数据集 r2 ego mean≈6.3--6.5，cap8 平均保留 95.7%/98.1%，仍是局部对象；下一轮只可换 patch 语义，不调融合容量
56. ~~TU typed radius-2 Stage A~~ → `results/luyin14/TUD_RADIUS2_VALIDATION_20260813.md`
57. **Mutagenicity radius-2 no-go**：typed patch types 424，但 TRUE=SHUFFLED=INIT≈0.751，重构改善未转分类增益
58. **NCI1 radius-2 9-fold**：FINAL−GIN `+5.0pt`（8/9）、TRUE−SHUFFLED `+1.8pt`（7/9），但 FINAL−INIT `-0.06pt`
59. **路线判断**：radius-2 joint patch representation 有稳定信号；普通无监督 K-SVD updates 仍无任务归因，不进入 cross-attention
60. ~~luyin16 导师固定特征概念复现~~ → `results/luyin16/MENTOR_CONCEPT_ROUTE_VERDICT_20260829.md`
61. **MolHIV fixed-feature 结论**：显式 topology+chemistry + XGBoost official-valid `0.7817`；但 chem/topology K-SVD update、S 外融合和 624D reconstruction proxy 全部无稳定增量
62. **停止项**：不继续扫 K/T、Beam、objective 或 fusion；导师约 0.80 路线必须先取得 69/624D 上游 schema、payload 与 best params，否则只能称 proxy
63. **统计对象补充**：slot-aligned `8×64+28×4=624` proxy 的 raw `0.7014` 高于全局统计 proxy `0.6649`，但仍无 S 外增量；occurrence-sum `0.5821`，不再猜聚合方式
64. ~~clean structural-role fusion~~ → `results/luyin16/CLEAN_STRUCTURAL_ROLE_FUSION_20260901.md`
65. **结构编码结论**：去掉 OGB degree/ring 后，rooted-WL factorized raw 在 official-train 三折相对 coarse `+5.47pt`、3/3 wins；coarse role 确实过弱
66. **迁移结论**：official-valid 上 centered 仍胜 shuffle `+1.56pt`，但仅比 `T+A` `+0.07pt`；factorized raw 比 `T+A` `-1.34pt`，依赖存在但无稳定标签增量
67. **停止项**：typed-edge 内部 `-1.64pt`、0/3 wins，exact topology valid type coverage 仅 `49.5%`；不扫 WL bins/rounds、exact orbit、path、attention 或 K-SVD
68. ~~task-aligned interaction / Optuna audit~~ → `results/luyin16/TASK_ALIGNED_INTERACTION_TUNING_20260901.md`
69. **超参数结论**：nested Optuna 使 `T+A` `+1.52pt`、centered `+0.23pt`、raw `-1.52pt`；raw/centered 相对 `T+A` 的 gap 分别缩小 `3.03pt/1.28pt`，均 0/3 folds 改善，未调参不是 interaction 失败的解释
70. **稀疏 residual no-go**：cross-fitted centered 胜 shuffle `+2.93pt`、2/3 wins，但比 tuned `T+A` `-0.57pt`、仅 1/3 wins；raw 比 baseline `-1.43pt`
71. **路线冻结**：不扩大 Optuna 或继续 selector/top-k/C 搜索，不重新打开 official-valid；rooted-WL `T+A` 保持主线，centered 只保留 dependence diagnostic
72. ~~rooted-WL frozen terminal test~~ → `results/luyin16/STRUCTURAL_ROLE_TERMINAL_TEST_20260901.md`
73. **单分支 test**：train+valid refit `T+A=0.7610`、centered `0.7716`，centered `+1.06pt`、4/5 seeds；调参改善 valid 但未消除 valid--test mismatch
74. **late fusion test**：预注册固定 50/50 `T+A+centered` paired-seed mean `0.7825`，十模型 probability ensemble **`0.7839`**；两个独立 expert 的互补性高于 feature-level residual
75. **终端冻结**：official test 历史上已被查看，本轮只算 controlled terminal evaluation；不再用该 test 调 view/权重/参数，下一步转向新 outer splits 上的 constrained late fusion
76. ~~exact rooted topology × conditional chemistry 快筛~~ → `results/luyin16/EXACT_CONDITIONAL_FUSION_20260901.md`
77. **融合层级结论**：exact/orbit 位置绑定 no-go；rooted-WL 在本轮观测 radius-2 patch 上无 exact collision。真正可复现的是同一 patch 内 topology 与 attribute multiset 的条件配对，true−patch-pair-shuffle `+2.36pt`、8/9 seed×fold wins
78. **预测边界与下一步**：conditional pairing 机制存在，但直接 joint XGBoost 未超过 `S+WL+attribute`；不扫 K/prototype/XGB，不看 valid/test，只允许一次 cross-fitted conditional residual gate
79. ~~luyin16 概念复现 → 机制 → 显式模型全路线~~ → `results/luyin16/EXPERIMENT_ROUTE_SUMMARY_20260829_ONWARD.md`；主判：**K-SVD=压缩器而非任务表征学习器**；机制阶段冻结在统计交互 + XGBoost
80. ~~显式模型路线（0903–0905）~~ → ZINC 精确 patch 路径 `0.1872/0.1381` → **层级关系上下文 `0.1816/0.1346`（当前最佳）**；MolHIV patch 路径 `0.8028/0.7852`，统一 OOV-only `0.8383/0.7727`（精确 token 反降到 0.805 valid）
81. **待答两个问题**：①跨中心交互在新骨架划分上是否仍稳定；②ZINC 0.13–0.18 差距来自 patch 身份 / 关系距离 / 读出统计，还是缺少全图上下文

82. ~~compact-v2 budget reallocation (09-07)~~ → `notes/compact_hybrid_v2_budget_reallocation.md`：valid 0.184158 @56（98,549 params）；refit-test 0.135362
83. ~~compact-v2 info-gap audit (09-07)~~ → `notes/compact_v2_information_gap_audit.md`：13 探针全 NO-GO；残差 = target 定义 long-cycle 项（valid 3.5% 分子 = 27.1% MAE mass）
84. ~~v3 ring-context conditioning NO-GO (09-08)~~ → `notes/compact_v3_context_conditioned_patch_representation.md` + `records/decisions/decision-compact-v3-context-conditioning-nogo-20260908.yaml`
85. ~~long-cycle audit GO (09-08)~~ → `notes/zinc_long_cycle_audit.md`：机制验证 + 不变量 oracle 上限 +0.0117
86. ~~compact-v4 global topology channel (09-09)~~ → `notes/compact_v4_global_topology_channel.md`：hinge valid +0.0141；seed-0 refit 口径 −0.0041（**协议口径分裂**，见 addendum）
87. **protocol correction + multi-seed confirmation (09-09)** → `notes/compact_v4_multiseed_protocol_confirmation.md` + `notes/reproducibility_cpu_determinism.md` + `records/claims/claim-929b3e29.yaml` + `records/decisions/decision-d5bf08b3.yaml`：primary benchmark protocol = train→valid selection→frozen checkpoint→test（**无 refit**；refit 降级为 secondary robustness）；seeds 0–3 串行 bit-verified：v4-hinge vs v2 mean paired Δtest **+0.0092（4/4 seeds）→ GO**（非 Strong GO；A 组 sign flip caveat）；下一步唯一推荐 **post-v4 residual audit**
88. ~~post-v4 residual audit (09-09)~~ → `notes/post_v4_residual_audit.md`：**NO CLEAR SECONDARY SIGNAL**（所有结构探针 < 0.003 gate；残差只是 z_SA/稀有度异方差 difficulty，无结构 v5 通道）
89. ~~OOF difficulty / heteroscedasticity confirmation (09-10)~~ → `notes/oof_difficulty_heteroscedasticity_audit.md` + `records/claims/claim-oof-difficulty-confirmed-20260910.yaml` + `records/decisions/decision-oof-difficulty-heteroscedasticity-go-20260910.yaml`：official-train K=5 OOF（frozen v4-hinge，seeds 0+1，valid/test 从未加载）= **difficulty GO 0.374 (5/5) + epistemic GO 0.393 (5/5)**；rarity 阶梯 0.133→0.417 MAE；2-seed ensemble −8.3%；结论：残差是**条件方差**，不是可修正偏差
90. ~~compact-v5 uncertainty-aware multi-quantile regression (09-10)~~ → `notes/compact_v5_multi_quantile_regression.md` + `records/claims/claim-compact-v5-mq-point-nogo-20260910.yaml` + `records/claims/claim-compact-v5-width-internalizes-difficulty-20260910.yaml` + `records/decisions/decision-compact-v5-multi-quantile-nogo-20260910.yaml`：**objective-only，representation 完全冻结**；非交叉 q10/q50/q90 head（+66 params）+ `2*pinball0.5 + λq*(pinball0.1+pinball0.9)`；对照 none/median-only **bit-identical** 于 v4/L1；λ∈{0.10,0.25,0.50} 预注册。Stage-1 seed0 = **Case A**（valid q50 +0.0114；width-|err| 0.254）→ 冻结 4-seed 确认仅 **Mild/Mild**（point +0.0037, 3/4；width 0.211, 4/4）→ 一次性 benchmark test **未 transfer**（0.139403 vs v4 0.136885，paired −0.0025，1/4 seeds）→ **NO-GO**。width 内化 OOF difficulty/epistemic 轴（0.376/0.408）但仅 Mild，**不足以开启 uncertainty-guided weighting**；compact-v4-hinge 仍是当前 candidate
92. ~~corrected token fragmentation & rarity audit (09-10)~~ → `notes/corrected_token_fragmentation_rarity_audit.md` + `records/claims/claim-corrected-token-fragmentation-20260910.yaml` + `records/decisions/decision-corrected-token-fragmentation-mixed-20260910.yaml`：**frozen paired historical-vs-corrected v4（train/valid only；test 从未加载），无训练**。corrected tokenizer 确实大规模切碎 exact vocabulary（r2 6,784→15,218, 2,363 split, mean multiplicity 2.24/max 104；parent 31→512, mean 16.5）并造成 1,065 newly-rare / 396 newly-OOV validation occurrence（631/1000 分子受影响）。**但 paired molecule degradation 并不由 fragmentation 定向解释**：所有 unconditional molecule-level Spearman |ρ| ≤ 0.06（frequency loss −0.025、newly-rare −0.023、newly-OOV −0.056，1–2/4 seeds 同向）；退化由 **baseline-difficulty redistribution** 主导（Spearman(baseline error, degradation) = −0.352，difficulty quintile 单调 +0.031→−0.040，4/4 seeds）——corrected 在 easy bulk 变差、在 hard tail 变好。G1/G2/G3/G4 = +0.0016/+0.0042/+0.0141/−0.0083（G4 反向；difficulty-matched G3 +0.0143、G4 −0.0089）。conditional-on-difficulty 后仅剩很弱、非特异的新增稀有信号（partial ρ≈0.10；already-rare/hist-OOV 同样方向）；rarity-difficulty 关系仅微增（0.305→0.322 / 0.349→0.372）；cross-seed disagreement 微升（+0.0036）但与任何 fragmentation metric 无关（|ρ|≤0.07）而仅与 degradation 相关（+0.31）；HybridEmbedding tier reallocation 16.6% occurrence downgrade 但与 frequency loss 共线（ρ=0.52）无独立信号。**Decision（Case D — MIXED）**：不开启 coarse-shared + exact-residual（也不开 coarse-to-fine / shared-low-rank）；下一步唯一推荐是诊断 baseline-difficulty redistribution（仅诊断，不训练）。simple frequency-fragmentation / statistical-sharing-loss 解释未达 gate。
91. ~~compositional patch sharing oracle (09-10)~~ → `notes/compositional_patch_sharing_oracle.md` + `records/claims/claim-compositional-patch-sharing-nogo-20260910.yaml` + `records/decisions/decision-compositional-patch-sharing-nogo-20260910.yaml`：**frozen 表征 oracle / 干预，不训练**；train-only patch bank + 146D 结构近邻 + 分层 donor 约束 + 5 个预注册变体（NN1/KNN8/Blend50/FrequentMean/RandomDonor）。baseline **4/4 seeds bit-exact**，unaffected molecules **bit-identical**。**NO-GO（Case E）**：所有变体 overall ΔMAE 为负（NN1 −0.0120、KNN8 −0.0109、Blend50 −0.0043、FrequentMean −0.0099、RandomDonor −0.0109；0/4 seeds 正向），危害集中在 rare/OOV affected 分子（rare-seen Δ −0.010..−0.025；OOV −0.012..−0.025），结构 KNN8 **不优于** frequent-mean/random-donor 对照，donor 距离无收益梯度。**附带审计发现**：frozen typed certificate（pynauty.certificate）**非单射**——730/6784 token 混合了不同 root atom（pynauty 的完整不变量还需 color-cell sizes）；记为独立正确性修复项，不作为 sharing 证据
93. ~~fast baseline-difficulty / typed-refinement redistribution audit (09-10)~~ → `notes/baseline_difficulty_typed_refinement_redistribution_audit.md` + `records/claims/claim-baseline-difficulty-typed-refinement-20260910.yaml` + `records/decisions/decision-baseline-difficulty-typed-refinement-20260910.yaml`：**快速决策审计（复用 frozen paired v4 predictions + official-train targets + historical v4 OOF residuals；无训练、test 从未加载）**，只做 H1/H2/H3 三测。**H1**：退化锁死于 historical difficulty（quintile +0.0312/+0.0180/+0.0116/+0.0020/−0.0397；per-seed Spearman(baseline, degradation) −0.218/−0.167/−0.268/−0.172，4/4 negative），且为**中心(bias)主导**——easy Δcentre_MAE +0.0213 > Δspread_MAE +0.0125，4-seed ensemble 在 Q1 自身也变差（0.0245→0.0489），easy ensemble 退化是个体退化的 ~87%（非 ~0）→ **不是方差/正则化损失**；movement 系统性（Q1 harmful 65.5% = wrong-direction 36.9% + overshoot 28.6%，magnitude-only 4.8%）。**H2**：构造 train-only typed-refinement informativeness（historical token 的 corrected children 的 support-weighted train target / historical-OOF-residual dispersion；2,363 split parent 中 707 eligible），与 corrected gain **无独立关系**（ensemble ρ −0.019；partial given difficulty −0.036；7 个 metric 变体 partial ∈[−0.069,−0.025]，0–2/4 seeds）——null 不是 metric 选择。**H3**：2×2 由 difficulty 而非 informativeness 排序（hard+low +0.0252 > hard+high +0.0129；easy+high −0.0268；interaction −0.0080）——high informativeness 反而**弱反预测**。**Decision（Case D — UNEXPLAINED REDISTRIBUTION）**：A 因 ensemble 自身退化而失败（easy ensemble 退化 = 个体 ~87%），B/C 因 informativeness 完全无信号而失败。**NO architecture GO**：关闭 tokenizer-derived architecture 分支（不做 coarse-to-fine / coarse prior / adaptive gate）；仅当出现预注册的、graph-side 可观测的 gate，或更换非 exact-identity 的 tokenizer 变更时才重启。
94. ~~compact-v6 topology–attribute factorization (09-10)~~ → `notes/compact_v6_topology_attribute_factorization.md` + `records/claims/claim-compact-v6-topology-attribute-factorization-nogo-20260910.yaml` + `records/decisions/decision-compact-v6-topology-attribute-factorization-nogo-20260910.yaml`：**唯一变量 = patch 表示**（historical v4-hinge path 逐位冻结 + 新增 shared、permutation-invariant、exact-token-free 的 `(type, coarse-rooted-automorphism-orbit role)` attribute encoder，零初始化 8D `e_attribute` 追加到 patch encoder 输入，+1,470 params → 101,083）。四模型矩阵：v4 / count-control / factorized-role / capacity-control（后三者等参数）。**seed-0 曾 Case A**（valid 0.156421 vs 0.170066，Δ +0.013645；factorized > capacity +0.0062 > count +0.0145），**但 multi-seed（seeds 0–2）不复现**：paired Δ +0.013645/−0.017017/+0.002487（mean **−0.000295**，median +0.002487，**2/3**）< 0.003 gate。**seed-0 收益是 hard-tail-for-bulk trade**：historical quintile Q1/Q2 combined **−0.017265**（bulk gate ≤+0.002 FAIL），Q5 +0.091709 —— 与 corrected tokenizer 同一 redistribution 指纹。**机制**：冻结 seed-0 模型对 attribute-type / role-association **within-patch shuffle 精确不变**（clean==shuffled MAE 0.156421，per-molecule max|Δ|=0.0；`e_attribute` norm std **2.3e-10** → branch collapse），count-control 两 shuffle 亦不变；count-only 无增益（seed-0 −0.000865，mean −0.002243 1/3），factorized−count mean +0.001948（1/3）< 机制 gate。唯一一致信号 factorized > capacity 3/3（mean +0.006180），但分支未被使用 → 属训练轨线而非 attribute 使用。**Decision（Case D at benchmark gate）**：关闭 topology–attribute factorization；不增大 attr dim / 不加深 MLP / 不加 role features / 不开 dictionary-KSVD。**revisit_if**：先诊断并修复零初始化导致的 branch collapse（step-0 上游死梯度 + weight decay 压权重），再以全新 seed sweep + 预注册 graph-side gate 重验。
95. ~~compact-v6 attribute-branch viability repair (09-10)~~ → `notes/compact_v6_attribute_branch_viability_repair.md` + `records/claims/claim-compact-v6-attribute-branch-collapse-and-viability-20260910.yaml` + `records/decisions/decision-compact-v6-attribute-branch-viability-nogo-20260910.yaml`：**先诊断 original collapse，再做唯一最小修复**。梯度 trace 证实原 compact-v6 分支 step-0 处 `e_attribute≡0`（零初始化 final fusion）→ 全部 upstream attribute 参数（embedding/MLP）与 patch_encoder 新增 8 列拿到**精确零梯度**，只有 `fusion.2` 能学；collapse 实际发生在 encoder MLP（frozen checkpoint `h_atom` occurrence std 5.2e-6 vs 输入 0.212，`e_attribute` 跨 patch std 7.6e-12 → 精确 shuffle-invariant）。optimizer 审计干净（14/14）、raw role 有信息（std≈0.30）、adversarial placement pair 有效。**最小修复**：只把 final fusion projection 由 `zeros` 改为 `normal(std=0.01)`（`attribute_fusion_init=small_normal`），其余逐位不动。**100-step viability gate 全过**（V1 patch_std 3.87e-4；V2 三层梯度活；V3 role-shuffle max 3.70e-5；V4 attr-zero max 1.38e-2）→ 分支确可训练/可影响预测。**fresh seed0（test blocked）**：valid 0.170209 vs v4 0.170066，Δ **−0.000143**（< +0.003 gate），且全量训练后分支**再次退化**（patch_std 3.62e-7；role/type shuffle mean≈1.2e-7），Q1/Q2 bulk degradation **+0.023831**（>0.002 FAIL，收益全在 Q5 +0.0511）——同一 redistribution 指纹。**Decision（Outcome 2）**：BRANCH REPAIRED, ARCHITECTURE NO-GO → **永久关闭 topology–attribute factorization**，STOP AT SEED0，不调 scale/dim/role/optimizer。8 tests 全绿。
96. ~~pair endpoint association witness audit (09-11)~~ → `notes/pair_endpoint_association_witness_audit.md` + `records/claims/claim-pair-endpoint-association-witness-nogo-20260911.yaml` + `records/decisions/decision-pair-endpoint-association-witness-nogo-20260911.yaml`：**frozen feature witness diagnostic；不改 backbone / 不训练新 backbone / 不改 pair encoder / 不训练新 pair 模块**。从真实代码确认当前 pair path：`u_i = pair_projection(patch_i) ∈ R^16`（pre-centre-update patch state），`pair_input = [u_i+u_j, |u_i-u_j|, (u_i⊙u_j)*gate, relation_encoder(23→32→16)] ∈ R^64` → `pair_encoder(64→64→16)` → `q_ij ∈ R^16`；relation 已含 `log1p(distance)` + 5 buckets。新 cache `frozen_state_export_v3_pair_endpoint`（在 corrected v2 上增加 `projected_patch_state` 与 raw `pair_relation`），**Gate 0 全过 5/5**（exported `u_i`==forward pair projection max **0.0**；reconstructed pair input==forward pair input max **0.0**；reconstructed `q_ij` max **0.0**；R 1.14e-5；pred 9.5e-7；batch 0.0/4.8e-7；node permutation witness 1.24e-7）。**Representation ambiguity（target-free）**：同 bucket 下 c-space 最近邻的 a-distance 仅为随机 pair 的 **0.317×**（relation-restricted 0.495×），Pearson(‖Δc‖,‖Δa‖)=**0.927**，close-c/far-a=0.0，k=16 邻域内 normalized conditional Var(a|c)=**0.252**——当前 pair map 已强预测 association，残余 ambiguity 温和。**Stage 1**（frozen OOF seed 0 × 5 folds × 1 init，复用同一 1200/400/400 hash split；B1 R→13→13→1=4135 params，B2/E [R;graph]→9→1=4177 params，+1.02%）：mean MAE B0 0.19316 / B1 0.19050 / B2 diag 0.19475 / E endpoint 0.19749；**mean ΔR = MAE(B1)−MAE(E) = −0.006994（95% CI [−0.01039,−0.00359]，P(>0)=0.0001，0/5 folds 为正）**；**mean Δdiag = MAE(B2)−MAE(E) = −0.002741（0/5 folds 非负）**；E 在 selection 上 5/5 差于 B1；bulk degradation mean +0.0050 / max +0.0112（>0.002 FAIL）；adapter 非塌缩（sensitivity min 0.338）。**Decision（Case C — R-only equally good/better）**：**NO-GO off-diagonal endpoint cross-coordinate association**；第二 backbone seed 与 Stage1b 未使用；**不做 learned projection / rank sweep / full 120D / attention / triplet / low-rank symmetric bilinear pair encoder**；compact-v4-hinge 保持不变。下一步（仅在有自身 task-relevant witness 后）：向计算图上游移动 → centre-incidence compression / one-shot centre update / higher-order interaction。10 tests 全绿。
97. ~~centre-incidence co-occurrence witness audit (09-12)~~ → `notes/centre_incidence_cooccurrence_witness_audit.md` + `records/claims/claim-centre-incidence-cooccurrence-witness-nogo-20260912.yaml` + `records/decisions/decision-centre-incidence-cooccurrence-witness-nogo-20260912.yaml`：**frozen feature witness diagnostic；不改 backbone / 不训练新 backbone / 不改 compact-v4 / 不改 pair encoder / 无第二次 centre update / 无 attention / 无 higher-order GNN**。从真实代码重新确认 centre update 计算图：pair unordered，每个 unordered pair 通过 `cat([source, target])` **同等贡献给两个 endpoint centres 各一次**；`bucket = min(max(d,1),5)−1`（5 buckets，d=1,2,3,4,5+）；每 (centre, bucket) 保留 `[mean(q) 16 ; population std(q) 16 ; log1p(count) 1] = 33D` × 5 = **165D**（std = `sqrt(clamp(count⁻¹Σq² − mean², 0) + 1e-8)`，空 bucket 精确置 0；log-count = `log1p(count)`）；centre update 真实输入 `[patch(48) ; context(165)] = 213D` → `Linear(213→60) → LayerNorm → ReLU → Dropout → Linear(60→48)`（末层零初始化）→ **residual** `patch ← patch + update`，unary readout 之后重算。新 cache `frozen_state_export_v4_centre_incidence`（同一批 frozen OOF checkpoints，未重训；v3 export 已足够重构 centre incidence，新增 pre/post centre-update patch states + 真实 165D contexts 使 gate 可直接检验），**Gate 0 全过 37/37 gate instances on 5/5 folds**（每 unordered pair 恰好一次给每个 endpoint，self-loop 0 / 重复 0 / Σ incidence = 2·n_pairs；bucket identity 与 relation one-hot **及** `log1p(distance)` 一致；reconstructed 165D context max **3.98e-4**（mean 9.09e-9，全部为近退化 cell 的 float32 方差消去）、`h'_i` **5.74e-6**、R **1.14e-5**、pred **9.5e-7**；incident row-order invariance **5.4e-19**；batch invariance R 0.0 / ŷ 4.8e-7 / context 0.0）。**Discarded quantity**：strictly upper-triangular population covariance `C_{i,b} = E[qqᵀ]−E[q]E[q]ᵀ`（denominator n，n<2 时 C=0）⇒ `c ∈ R^120`；固定 deterministic orthonormal 120×8 projection（seed 20260912，**不学习**）→ 每 bucket 跨 centres `mean+std` → `C_graph ∈ R^80`。**关键 control：`M_graph`**——用完全相同管线（同上 8D、同 graph 聚合、同 adapter 接口、同 80D）但从**模型已有的** `m = [mean;std;log-count]`（直接取自真实 forward context）构造，33×8 projection（seed 20260913）。**Stage 1**（frozen OOF seed 0 × 5 folds × 1 init，复用同一 1200/400/400 hash split）：mean MAE B0 **0.193160** / B1 matched-budget R-only **0.202477**（302→14→1, 4257 params）/ B1_hist 历史 R-only **0.190499**（302→13→13→1, 4135 params）/ B2 marginal control **0.200194** / E covariance **0.201555**（B2 与 E **架构完全相同** 382→11→1, 4225 params；B1 差 +0.75%）。**mean ΔR = +0.000922（95% CI [−0.00447,+0.00619]，P(>0)=0.64，3/5）**，**mean ΔR_hist = −0.011056（CI [−0.01610,−0.00612]，**0/5**）**，**mean ΔM = MAE(B2)−MAE(E) = −0.001361（CI [−0.00569,+0.00279]，P(>0)=0.27，**2/5**）**——decisive 负结果：covariance witness **差于** matched marginal control（selection 5/5 更差；common-input bulk max +0.0077 > 0.002 FAIL）；adapter 非塌缩（sensitivity min 0.601，pred std 1.84）。**Representation ambiguity（target-free，非 performance gate）**：m-space 最近邻在 covariance space 的距离仅为随机 cell 的 **0.282×**；Pearson **0.596**、Spearman **0.684**；normalized conditional Var(c | 16-NN m) = **0.285** ⇒ 被丢弃的统计量**确实存在**（数学非唯一性在真实 frozen states 中可测），但**不具备 task relevance**——正是本阶段要做的区分。**文档化的 depth confound**：历史 2 层 R-only head 比 matched 1 层 head 强 **+0.01198** mean MAE（fixed non-swept sensitivity，5/5 folds），故 B1_hist 仅作 robustness evidence；决定性 E-vs-B2 检验 **depth-matched 且 parameter-matched**，不受此 confound 影响（confound 方向**不利**于负结论 ⇒ 结论保守）。**附带发现**：frozen pair encoder 末层为 ReLU ⇒ `q_ij` 有 **86–97%** 精确零 entries，仅 **3–29%** 的 n≥2 cell 有非零 covariance row（已由上一轮 v3 export 独立复现）。**Decision（Case B — centre marginal re-access only — + Case C evidence）**：**NO-GO within-centre cross-channel relation co-occurrence**；第二 backbone seed、Stage-1b init、channel-shuffle surrogate **均未使用**（surrogate trigger 未满足）；**不做 covariance/joint-relation/centre-incidence 架构、不做 full covariance、不放大或学习 projection、不做 attention over relations / DeepSets centre encoder / second centre update / relation-set Transformer**，也**不再登记第三种「沿计算图补一个明确缺失统计量」的探针**；下一步只能在**范式层面**重新提问（information accessibility / learnable parameterization / one-shot centre update 作为 function-class 假说），且必须自带预注册 witness。compact-v4-hinge 保持不变。12 tests 全绿。
98. ~~optimized-manifold broad frozen-state sufficiency screen (09-19)~~ → `notes/optimized_manifold_broad_state_screen.md` + `records/claims/claim-optimized-manifold-broad-state-inconclusive-20260919.yaml` + `records/decisions/decision-optimized-manifold-broad-state-inconclusive-20260919.yaml`：**cheap screening gate（不训练任何 backbone；不访问 official test；不改 compact-v4/tokenizer/topology/loss）**。复用两个已训练 optimized compact-v4-hinge checkpoint（seed0 valid 0.146420、seed1 valid 0.149332，训练成本已付），新 export 版本 `optimized_frozen_state_export_v1`；参数匹配的历史 reader 原样复用（B1 R-only 302→13→13→1 = 4135 params；E broad state = 4145 params，mismatch **0.24%**）；official-train deterministic hash split **7200 adapter-fit / 800 adapter-selection / 2000 train-probe**；official valid 在 adapter checkpoint 锁定后只评估一次。**Gate 0 全过 19/19 两 seeds**（true pre-head R **302D**；valid prediction anchor 与 checkpoint valid MAE **精确 0.0**；reconstructed R 6.1e-5/8.4e-5；pred 1.9e-6；pair/h′ graph grouping 与 coverage 全过；batch invariance R 0.0 / ŷ 4.8e-7；node relabelling R 2.3e-5）。**Init-0**：seed0 B0 0.146420 / B1 0.143495 / E 0.143125，**Δstate = +0.000370**；seed1 B0 0.149332 / B1 0.146420 / E 0.145270，**Δstate = +0.001150**；mean **+0.00076**（两 seed 同向但远低于 +0.003）→ **BORDERLINE**，触发预注册第二 adapter init：seed0 +0.000179、seed1 **−0.000513**（sign flip），combined mean **+0.00030 ≤ +0.0005**。common-input bulk safe（seed0 +0.00041、seed1 −0.00028；gate ≤ +0.002）；state branch **alive**（state-zero / within-molecule permutation sensitivity **0.026–0.121**）。**Verdict：`INCONCLUSIVE_DO_NOT_BUILD_OOF`（mandate Case C）**：不构建 optimized OOF、不重跑 endpoint/covariance/triad、不加第二 backbone seed；历史 NO-GO 永久保持关闭。bottleneck map = historical state audit NO-GO + optimized broad screen INCONCLUSIVE（no OOF）；下一步只在**范式层面**（representation-family / structural-computation redesign）重新提问。附带观察：refit R-only head 相对 frozen head 在 valid 上强 **+0.0033/+0.0041**（late-readout adaptation，非 state effect）。13 tests 全绿。
99. ~~same-budget structural computation gap analysis (09-20)~~ → `notes/structural_computation_gap_analysis.md` + `results/structural_computation_gap_analysis/`（14 文件 + `answers_q1_q20.json` + `information_path_examples.md` + 3 图）+ `records/claims/claim-structural-computation-gap-analysis-20260920.yaml` + `records/decisions/decision-structural-computation-gap-analysis-caseA-20260920.yaml`：**纯静态分析（不训练 / 不跑 official test / 不重训 compact-v4 / 不重跑 endpoint·covariance·triad / 不实现 CIN / 不实现 message passing / 不加 feature / 不做 compact-v7）**。从**真实代码**重建 compact-v4 计算图：单次前向（single pass），patch = 半径-2 ego-net（每原子一个，valid 均 23.083），**完整无序 pair 集**（均 264.78），`q_ij = pair_encoder([u_i+u_j, |u_i-u_j|, u_i⊙u_j*gate, 23D relation])` **只算一次**，随后被压进 per-(centre,bucket) `[mean 16 ; pop.std 16 ; log1p(count) 1]=33D ×5 =165D` 与 graph bucket moments；centre update 为 **one-shot**（zero-init residual，15,888）；`R = [unary 97 ; pair 165 ; global 32 ; topology 8] = 302D`；**没有任何 per-cycle 对象**，cycle 信息只是图级 25D hinge 向量（552 params）。参数分布：typed_token_embedding 36,420（36.56%，是**恒等查表**）> graph_head 21,633 > center_context 15,888 > patch_encoder 14,192 > pair_encoder 5,328 … 总 **99,613**（selection 口径）。从 primary source 核实 higher-order reference **CIN-small**（Weisfeiler and Lehman Go Cellular, NeurIPS 2021, arXiv:2106.12575v3；官方 repo twitter-research/cwn，config `cwn-zinc-small.sh`，max_dim 2 / 2 层 / hidden 48 / max_ring_size 18 / use_coboundaries）：induced-cycle ring lifting 出显式 **2-cell**，每个 0/1/2-cell 保留持久 hidden state，在 **2 层 rank-aware cellular message passing**（boundary + upper-adjacency/coboundary，Eq.4）中沿**显式 incidence 表**更新，per-rank sum 读出；官方配置实跑 **130,945** params（paper 仅说 “in the order of 100k”，标 PARTIALLY VERIFIED；88% 参数是 message-passing 算子，与 compact-v4 的“查表 + 单次算子”形成对照）。**关键证据**：官方 Table 9 No-Rings 消融 **CIN No-Rings small 0.174±0.006 → CIN-small 0.094±0.004**（环主导增益）；但 **CIN No-Rings（2 层持久 edge-state message passing、无环）比我们的单次模型还差** ⇒ 持久化/深度本身不足，**显式 higher-order 对象**才是关键。`benchmark_comparability` = **PARTIALLY VERIFIED**（dataset/split/target/metric/edge-features 逐项 VERIFIED 相同；seeds/horizon/scheduler/参数数/hardware NOT VERIFIED）⇒ 严格遵守硬规则：性能差（0.125 vs 0.094）**仅作为 headroom 证据，绝不当机制证明**。审计引入术语（非文献）：**Structural Persistence**（对象是否保持独立身份并持续参与学习变换）与 **Incidence Persistence**（“哪个低阶对象属于哪个高阶对象”是否始终可被后续学习计算查询）。**判定：Decision Case A — ONE REPRESENTATION HYPOTHESIS AUTHORIZED（仅设计，不运行）**。Top-1 可证伪假说 = **incidence-preserving structural persistence of explicit higher-order objects**：reference 把 cycle 实例化为带持久 learned state 的 2-cell 并沿显式 incidence 更新；compact-v4 把 cycle 压成图级向量、并在一次 encoder pass 后把 relation 对象身份丢进 moment。与既有 NO-GO 的关系：endpoint / covariance / triad / frozen readout / optimized broad-state 都是在**冻结状态**上换 reader / 加统计量（**改统计量集**），本假说**改计算图**（新对象类 + 持久 state + 显式 incidence）——因此不等价。最小证伪架构 `compact-v4-cell`（**设计，未运行**）：复用现有 bounded induced-cycle 枚举（k 预注册固定，**不扫**）+ `CellInit`(boundary patch-state 均值的小 MLP) + **恰好 2 轮预注册 rank-aware incidence rounds** + 池化 cell block（97D）追加到 R（**302→399**），Δ≈8–12K ⇒ **~108–112K ≤ 120K**；其余（radius/tokenizer/embedding/pair/relation encoder/loss/protocol/topology channel/head 约定）逐位不变。**预注册 seed0 门**：优化协议（Adam lr 1e-3、wd 1e-5、batch 128、240 ep、patience 40、无 scheduler、L1、best-valid），baseline = optimized compact-v4 seed0 valid **0.146420**，主门 **Δvalid ≥ +0.004**，机制存活（cell-shuffle 敏感度 > 1e-3 且 true-incidence 优于 cell-shuffle），bulk 安全 ≤ +0.002；**只花 seed0**，通过才买 seed1，默认绝不 seed3；**official test 永不加载**；对照 = **multiset-preserving 但破坏 incidence 的 cell-shuffle** + 参数匹配的 global cycle-statistic 对照（用于把 incidence persistence 与“多加环统计量”分开）。**诚实标记的方法论次条件**：reference 版本本质是 2 层 cellular message passing，最小架构需要少量固定轮数的 rank-aware 传播；**若 seed0 显示增益需要大量迭代轮数，则改判 Decision Case B（method-philosophy tradeoff），不授权实现**。附带结论：G7（全局结构先验）**不是** gap——compact-v4 反而更强（topology GO optimized Δtopo +0.019846），故新假说必须与该通道**互补**而非替代。`tests/test_structural_computation_gap_analysis.py` 12 tests 全绿（参数分解与真实 audit 对齐并断言 99,613/130,945；audit 不改前向；pair 集 = C(n,2)；来源可追溯；official test 从未加载）。下一步：执行 `compact-v4-cell` 的 seed0 证伪（唯一被授权的下一阶段运行）。
100. ~~compact-v4 learned relation-to-centre composer P1 (09-11)~~ → `notes/compact_v4_learned_centre_composer.md` + `results/compact_v4_learned_centre_composer/`（architecture_lock + baseline_inventory + parameter_audit + initialization_match + integrity_gates + compute_audit + stage1_p1_seed0 + stage1_decision + common_input_bulk + composition_diagnostics + final_decision + answers_q1_q20 + curves/ + figures/）+ `records/claims/claim-compact-v4-learned-centre-composer-nogo-20260911.yaml` + `records/decisions/decision-compact-v4-learned-centre-composer-nogo-20260911.yaml`：**唯一变量 = pair→centre composition**（保留 v4 全部其余路径；把固定的 `[mean(q)16 ; pop.std(q)16 ; log1p(count)1]=33D` per (centre,bucket) 加上一条**残差** learned composer：shared `phi=Linear(16,24)->ReLU->Linear(24,24)`；per-(centre,bucket) permutation-invariant mean `z_ib=mean_{q in Q_ib} phi(q) in R^24`；`u_ib=[z_ib ; log1p(n_ib)] in R^25`；shared `rho=Linear(25,33)->ReLU->Linear(33,33)`（末层零初始化）；`a^P1=a^fixed+rho(u)`；空 bucket residual 严格为 0；R 仍 302D；centre-update 输入仍 213D；**102,601 params = 99,613 + 2,988**）。**P1 seed0 valid 0.147229（best ep 151，191 ep，15,089 steps，1300s）vs 复用 optimized-v4 seed0 0.146420 → Δarch0 = −0.000809 ≪ +0.004 gate → Case A NO-GO**。branch 真活且被训练（residual-zero 推断消融 max|Δ|=0.531，mean 0.037；phi_grad max 6.0e-3、rho_grad max 0.177；learned residual norm 0.078→0.81；residual/fixed ratio ckpt 0.373）；**17 个 Stage-0 integrity gates 全过**（fixed pooling 复用原实现 source SHA 一致；零初始化/禁用时与 baseline 精确 0.0；relation-order 置换不变 4.2e-7 且 composer 确激活；空 bucket 精确 0；48 个共享 tensor 与 seed0 baseline init 精确一致 max diff 0.0）；common-input bulk safe（v4 0.09472 vs P1 0.09358，diff −0.00114）；无优化异常。**matched post-pooling capacity control（psi 33->44->33，+2,981，mismatch 7=0.23%）未购买；seed1 未运行；seed2/3 禁止；official test 永不加载**。compute 单独报告：φ 每 relation 调一次（均 272.5 pairs/molecule），128-mol batch 前向 19.79→30.02 ms（**+51.7%**）@ 仅 +3.0% 参数。禁止 rescue：attention / 加宽 phi–rho / 改 pooling / bucket embedding / centre conditioning / relation refresh。`tests/test_compact_v4_learned_centre_composer.py` 22 tests 全绿。下一步：P1 失败**不**自动触发 P2；仅在独立预注册下才讨论 P2 one-shot relation refresh。
101. ~~compact-v4 one-shot relation refresh P2 (09-11)~~ → `notes/compact_v4_relation_refresh.md` + `results/compact_v4_relation_refresh/`（Stage A: baseline_inventory + staleness_probe_manifest + staleness_audit_lock + staleness_integrity + relation_drift + pair_summary_drift + prediction_shift + stageA_decision；Stage B: architecture_lock + parameter_audit + initialization_match + compute_audit + stageB_p2_seed0 + stageB_decision + common_input_bulk + mechanism_diagnostics + final_decision + answers_q1_q20 + curves/ + figures/）+ `records/claims/claim-compact-v4-relation-refresh-nogo-20260911.yaml` + `records/decisions/decision-compact-v4-relation-refresh-nogo-20260911.yaml`：**唯一变量 = relation stage ordering**（保留 v4 全部其它路径；`h0→q0→A→h1` 不变，然后**用完全相同的 `pair_projection`/`pair_encoder`/`relation_encoder`/`distance_gate` 张量**对 `h1` 再算一次 `q1=Q(P(h1),r)`，**refresh count = 1**，最终 pair moments 只 pool `q1`；`q0` 仍负责 centre update，`q1` **不**再进入 centre update（无 `h2`），**无 alpha 混合、无 concat [q0;q1]、无独立 refresh encoder**，R 仍 302D，**总参数 99,613 = 99,613，Δparams = 0**）。**预算策略：先做零训练 Stage A。** Stage A（label-free；复用 optimized-v4 seed0 checkpoint sha256 `60b7d297…`；2000 分子 official-train train_probe，来自**已存在的** optimized-manifold deterministic hash split，`target-independent`，与 frozen manifest 完全一致；**11 个 A0.x integrity gates 全过**：checkpoint fingerprint、refresh-off↔parent 预测重构 **0.0**、h0/h1=48、q0/q1=16、用共享张量功能重构 q1 **0.0**、pair ordering 一致、`_pool_pairs` 实现同一、无额外 state key、无 label、official test 从未加载）显示 pre-context `q0` 与 post-context `q1` **不可数值互换**：median `cos(q0,q1)` = **0.9827**、median normalized L2 = **0.1820**（原始 mean 被 21.7% 精确零 `q0` 行污染，剔除退化行后 mean 0.8301）、median graph pair-summary drift = **0.1988**（mean 0.2099，p90 0.3021）、mean `|Δprediction|` = **0.0670**（median 0.0453，p90 0.1340，max 1.9917）→ 三条 near-identity 条件**均不满足** → **ADVANCE**（仅授权一次 seed0）。**P2 seed0**（scratch 训练；优化协议冻结：Adam lr 1e-3、wd 1e-5、batch 128、240 ep、patience 40、无 scheduler、L1、best-valid）valid **0.144457**（best ep **156**，196 ep，15,484 steps，1382s）vs 复用 optimized-v4 seed0 **0.146420** → **Δ_P2,0 = +0.001963 < +0.004 gate**（**sub-threshold positive**）。**Case B — ONE-SHOT RELATION REFRESH NO-GO**。branch 真活且被训练（refresh-off 同 checkpoint valid **0.175124**，mean `|Δpred|` 0.0932、max 1.6828；`q1`-path grad 非零 max 0.0418；`pair_encoder` grad max 5.70；训练期 qcos≈0.686、pair_summary_drift≈0.304）；common-input bulk **safe**（v4 0.09472 vs P2 0.09373，diff −0.00099）；无优化异常（无 NaN，best ep 156/196 非边界）；参数中性但**非** compute 中性（128-mol batch 前向 18.48→29.81 ms，**+61.3%**，272.5 pairs/molecule，peak RSS 2026 MB）。**seed1 未购买；seed2/3 禁止；official test 永不加载**。禁止 rescue：第二次 refresh/depth sweep（T=2,3,4）、独立 q1 权重、alpha 混合、concat、attention、更大 q dim、learned pooling、P1 composer、新 topology。**战略含义**：P1（learned centre composer）＋compact-v4-cell（cycle object）＋P2（relation refresh）**连续三次局部修复 clean NO-GO**，削弱「compact-v4 只差某个局部 computation ordering 小缺陷」的解释；下一步应转向 **paradigm-level representation-family / parameter-allocation redesign**，而非继续逐个 patch。`tests/test_compact_v4_relation_refresh.py` 24 tests 全绿。
102. ~~compact-v4 parameter allocation & representation leverage audit (09-12)~~ → `notes/parameter_allocation_representation_leverage.md` + `results/parameter_allocation_representation_leverage/`（22 文件 + 4 图 + `answers_q1_q20.json`）+ `records/claims/claim-parameter-allocation-representation-leverage-20260912.yaml` + `records/decisions/decision-parameter-allocation-representation-leverage-caseC-20260912.yaml`：**analysis-only / zero-full-backbone-training 审计**。复用两个已训练 optimized compact-v4-hinge checkpoint（seed0 valid 0.146420、seed1 valid 0.149332；99,613 params；R 302D；不训练任何 backbone；official test 从未加载）与 deterministic official-train split（7200 fit / 800 selection / 2000 train-probe）。**真实参数账本**（从 instantiated model 重算，不用旧硬编码）：`typed_token_embedding` 36,420（**频率自适应 hybrid**：768 full 16D rows + 6,017 rank-4 factorized rows + 16×4 projection）> `graph_head` 21,633 > `center_update` 15,888 > `patch_encoder` 14,192 > `pair_encoder` 5,328 > `global_encoder` 3,136 > `relation_encoder` 1,360 > `pair_projection` 768 > `topology_encoder` 552 > `parent_embedding` 256 > `distance_gate` 80，Σ = **99,613**（class：identity_storage 36,676/36.82%、shared_operator 37,616/37.76%、late_readout 21,633/21.72%、global_structural_prior 3,688/3.70%）。**DONOR**：graph head 为 **H-STRONG** —— 单一固定 4,135-param raw small head（302→13→13→1，direct `ŷ=f(R)`、raw R、L1/Adam lr 1e-3 wd 0、7200 fit/800 selection、horizon 800、head_seed 0、无 search）seed0 **0.143208** vs H0 0.146420（Δ −0.003213）、seed1 **0.147754** vs 0.149332（Δ −0.001578），mean −0.002396，同时移除 17,498 params（**80.9%**）→ 与历史 graph-head late-adaptation 一致（原 head 对最终 R under-adapted）；但这是 valid-selected checkpoint 上的 **donor screening**，非 clean end-to-end compression proof。**LOOKUP**：**L-NO** —— effective 16D table 99% energy rank 15/16，768 frequent rows 真·full-rank-16（stable rank 11.16），6,017 rare rows 已 rank-4；global rank-15/16 factorization 的 hypothetical saving 为 **负**（−65,595 / −72,396），frozen r99/r999 预测漂移与 valid 变化可忽略（seed0 r99 drift mean 0.0119、valid Δ +0.00033；seed1 r99=16 → 0）。**RECIPIENT**：无 **R-SUPPORTED** —— pair path / center_update **R-DISFAVORED**（P1/P2/covariance/triad/endpoint NO-GO 等价封闭）、higher-order structural persistence **R-DISFAVORED**（compact-v4-cell NO-GO）、patch_encoder 仅 **R-UNCLEAR**（h0 PR/d=0.157 只是 bandwidth evidence）、topology/global out-of-scope（正结果 GO）。附带：token occurrence 高度集中（top-10% rows 覆盖 **89.0%** occurrences；80/90/95% 只需 280/770/1,616 rows），但 **rare ≠ wasted**，禁止 frequency pruning；activation 各 state 均窄（R PR/d=0.011，q 0.278，h0 0.157），只作 bandwidth 证据；gradient 仅描述性局部敏感性，不作 importance 排名。**判定：COMPRESSION-ONLY OPPORTUNITY（mandate Case C）** —— donor 强支持但无科学上可辩护的 representation-forming recipient，**不授权任何 budget-reallocation 性能架构、不开启下一 full-training run**；唯一合法的后续动作（若做）是 compactness/compression 实验（head 预算 ~4–8K），且不得把释放参数投向 pair/centre encoder。`top1_reallocation_hypothesis.json` = `authorized: false`（不伪造 candidate）。15 tests 全绿（ledger Σ=99,613、tensor 不重复计数、instrumentation 不改 forward、frequency 仅来自 train inputs 且不读 target、SVD rank 机械来自 99%/99.9% energy、perturbation 不改非 lookup 参数、checkpoint fingerprint、R=302D、small-head 固定 4135 无 search、split 仅 official-train、valid 不参与 head selection、official test 从未加载、gradient 不更新参数）。
103. ~~compact-v4 small-head end-to-end compression confirmation (09-12)~~ → `notes/compact_v4_smallhead_e2e_compression.md` + `results/compact_v4_smallhead_e2e/`（architecture_lock + baseline_inventory + baseline_anchor + parameter_audit + initialization_match_seed0/seed1 + integrity_gates + compute_audit + stage1_smallhead_seed0 + stage1_decision + stage2_smallhead_seed1 + stage2_replication_decision + common_input_bulk_seed0/seed1 + two_seed_summary.csv + representation_drift + final_decision + answers_q1_q18 + curves/ + figures/）+ `records/claims/claim-compact-v4-smallhead-e2e-replicated-non-inferior-20260912.yaml` + `records/decisions/decision-compact-v4-smallhead-e2e-replicated-non-inferior-20260912.yaml`：**唯一变量 = graph head**（其余 tokenizer / historical aliased rooted-topology representation / embedding / patch encoder / pair projection / pair encoder / pair construction / distance features / centre mean-std-logcount pooling / centre update / final unary & pair moments / global descriptors / topology branch / R=302D / loss / optimizer / protocol 全部逐位冻结）。small head = 历史审计过的 fixed raw `Sraw`/`Hsmall`（`GenericReader`，**302→13→13→1，raw R，direct ŷ=f(R)，ReLU，无 LayerNorm / dropout / standardisation / residual**；head init `head_seed=0`；**无 head 搜索**）。**参数**：baseline 99,613（head 21,633）→ candidate **82,115**（head 4,135），Δparams **−17,498**（whole model **−17.57%**，head **−80.89%**）。**Stage 0 全过**：baseline_anchor 用现有 optimized seed0 checkpoint 精确复现 official valid **0.14642022556537995**（不重训）；G0.1–G0.10 全 true；seed0 与 seed1 各 **40 个共享 tensor max_abs_diff 0.0 + hash match**（先 instantiate canonical baseline、再 instantiate small-head、exact copy、最后 head_seed 0 独立初始化）；pre-head **R 在真实 128-mol valid batch 上 bit-identical（两 seed，max abs diff 0.0）**；G0.9 协议逐字段一致；G0.10 official test blocked。**Stage 1 seed0**（from-scratch joint；Adam lr 1e-3、wd 1e-5、batch 128、240 ep、patience 40、无 scheduler、L1、best official-valid、单阶段；loader seed `seed+91011`/`seed+91012` 与 canonical 一致）：valid **0.145334**（best ep **176**，216 ep，17,064 steps，1028 s）vs **0.146420** → **D0 = −0.001086 ≤ +0.002 PASS**（无优化异常）。common-input bulk（target-independent，train 80th percentile rare_le5）seed0 v4 0.094721 vs small 0.086754（diff **−0.007967**，安全）。**购买 seed1**（D0 通过）：shared init 与 canonical baseline seed1 exact match（40 tensor，max diff 0.0，hash 与 seed0 不同，未重用 seed0），valid **0.138059**（best ep **143**，183 ep，14,457 steps，895 s）vs **0.149332** → **D1 = −0.011273 ≤ +0.002**；**mean(D0,D1) = −0.006180 ≤ +0.0015**，两 seed 均 < 0 → **Decision Case D：REPLICATED_NON_INFERIOR_COMPRESSION_WITH_FAVORABLE_POINT_ESTIMATES**。bulk seed1 diff **−0.006829**（安全）。**compute 单独报告**：128-mol 前向 20.18→21.05 ms（+4.3%，backbone 主导，**不**假设 17.6% 参数减少带来同比例加速）；training 1028/895 s。**representation drift（descriptive，非 gate）**：seed0 selected R 的 normalized L2 drift mean **1.294**、mean cosine **0.172**（joint training 改变 backbone 表征，符合预期，不否定 compression）。**未重训 baseline；未运行 seed2/seed3；释放的 17,498 参数未 reallocate；official test 从未加载**；compaction 结果只成为更干净的 compact baseline candidate（~82.1K），未来只有在独立预注册且有 R-SUPPORT 的 representation hypothesis 下才可花费释放预算。`tests/test_compact_v4_smallhead_e2e.py` 15 tests 全绿。

104. ~~compact-v4 stagewise representation collision audit (09-12)~~ → `notes/stagewise_representation_collision_audit.md` + `results/stagewise_representation_collision_audit/`（34 文件：protocol/checkpoint/split/stage inventory + instrumentation_integrity + set_sketch_lock + normalization_stats + projection_manifest + phaseU_integrity + 4 个 neighbour JSONL + 44 个 per-stage shard + neighbor_manifest_hashes + target_metric_lock + random_neighbor_baseline + target_far_threshold + stage/contrast metrics CSV + bootstrap_intervals + projection/k robustness + bottleneck_decision + top1_representation_hypothesis + answers_q1_q20 + final_decision + 3 图）+ `records/claims/claim-stagewise-representation-collision-audit-caseE-20260912.yaml` + `records/decisions/decision-stagewise-representation-collision-audit-caseE-20260912.yaml`：**analysis-only / zero-full-backbone-training**。复用两个已训练 **82,115-param compact-v4-smallhead** checkpoint（seed0 valid 0.145334、seed1 0.138059；R 302D；不训练任何 backbone；official valid 不参与 stage selection；**official test 从未加载**）与 official-train deterministic `7200/800/2000` split（reference=adapter_fit 7200、primary=train_probe 2000、replication=adapter_selection 800）。**从真实代码导出实际 module 张量**：S0 `patch_encoder_input` 170D、S1 `h0` 48D、S2 `pair_encoder_input` 64D、S3 `q0` 16D、S4 `centre_update_input` 213D、S5 `h1` 48D、S6 `unary_fixed` 97D、S7 `pair_fixed` 165D、S8 `global` 32D、S9 `topology` 8D、S10 `R` 302D；instrumented forward 与原 `encode` **逐位一致**（R 与 yhat max abs diff **0.0**），10 个 U0.x integrity gate 全过。**Phase U（无标签）**：variable-size stage 用固定、target-independent、set-aware 的 deterministic projected-quantile sketch（24 个锁定 Gaussian unit projection × 9 quantile + `log1p(count)` = **217D**；base_seed 1729；stage seed `sha256(base_seed|stage)`；empty set 全 0 无 NaN；再用 7200 reference sketches 做 per-coordinate z-score）；fixed-size stage 用 7200 reference per-coordinate z-score 后 `||a-b||/sqrt(d)`；primary k=8；normalization/projection/sketch/distance/nearest-neighbour **全部在 target 读取前完成并 SHA-256 hash-lock**。**Phase Y（冻结邻居后）**才读 official-train target；`tau_far=3.3656`（固定 random reference pair 的 |Δy| 80 分位），V_rand≈2.08--2.12。**Primary η=V_8/V_rand（probe，seeds 0/1）**：**A patch encoder (X_patch→h0) MATERIAL_GEOMETRY_GAIN −0.1927/−0.1820**；**B pair encoder (X_pair→q0) MATERIAL_GEOMETRY_GAIN −0.0397/−0.0336**；**C centre contextualization (X_centre→h1) SEED_UNSTABLE sub-threshold +0.0071/−0.0115**；**D1 unary graph compression (set(h1)→U97) MATERIAL_GEOMETRY_GAIN −0.0883/−0.0526**；**D2 pair graph compression (set(q0)→P165) 稳定 sub-threshold 正向 +0.0113/+0.0432（seed0 < +0.02 gate）且 collision-inconsistent（Δcollision 在所有 seed/split 为负）**。**没有任何 contrast 达到预注册的双 seed material degradation gate** → **Decision Case E：NO CLEAR STAGEWISE REPRESENTATION COLLISION**；`top1_representation_hypothesis.json` = `authorized_for_design: false`、`next_full_training_budget: "not yet authorized"`；robustness rerun 未触发（未伪造结果）。**结论**：「compact-v4 某一阶段破坏 target-relevant geometry」这一默认解释**不被支持**；patch/pair encoder 与 unary moment 甚至**创造/保留** target-aligned geometry（descriptive，不授权加宽）；下一步应转向 **generalization / regularization / training stochasticity / scalar-target-mapping geometry / finite-data sample efficiency**，而不是再加 structural module。**geometry evidence, not information-theoretic proof**。`tests/test_stagewise_representation_collision_audit.py` 23 tests 全绿。
105. ~~Top-5 checkpoint aggregation stabilization audit (09-12)~~ → `notes/top5_checkpoint_aggregation_stabilization.md` + `results/top5_checkpoint_aggregation_stabilization/`（aggregation_protocol_lock + checkpoint_inventory + top5_manifest_I0T0/I1T1 + soup_construction_I0T0/I1T1 + soup_state_hashes + integrity_gates + development_reuse_probe_I0T0/I1T1 + development_reuse_summary + prediction_disagreement + weight_space_diagnostics + confirmation_lock + official_valid_I0T0/I1T1 + official_valid_summary + paired_bootstrap + compute_audit + final_decision + answers_q1_q20 + states/ + 3 图）+ `records/claims/claim-top5-checkpoint-aggregation-stabilization-20260912.yaml` + `records/decisions/decision-top5-checkpoint-aggregation-stabilization-20260912.yaml`：**zero-new-backbone-training checkpoint-estimator 实验**（不训练任何 backbone；不访问 official test；不改 architecture/representation/tokenizer/loss/optimizer；不扫 K；不做 EMA/SWA/greedy/weighted soup）。复用 **I0T0 / I1T1** 两条 82,115-param compact-v4-smallhead 轨迹（240 / 213 个 per-epoch snapshot，全部存在且 SHA-256 校验通过）与 frozen official-train deterministic **7200/800/2000** split（`assignment_sha256 7b6704d4…`）。**K=5 在任何新评估前冻结**；Top-5 严格按 **800-selection MAE** 升序、tie = earliest epoch：**I0T0 = 232/215/223/219/228**（0.152610/0.152810/0.154181/0.154310/0.154399）、**I1T1 = 173/197/183/170/195**（0.154226/0.154724/0.154912/0.155499/0.156336）；无 epoch spacing、无 diversity selection。三个 locked estimator：**RAW = e_1**；**ENSEMBLE = 5 个 checkpoint 预测的等权算术平均**；**SOUP = 5 个 state_dict 的等权算术参数平均**（仍 **82,115 params**；46 个 trainable float32 tensor、**0 buffers**、全 tensor finite、`load_state_dict(strict=True)` 无 missing/unexpected）。**Stage A**（development-reuse 2000 probe，已参与上一轮 hypothesis formation，只作 descriptive、无 continue/stop gate、不改任何规则）：I0T0 raw **0.131679** / ens **0.127050**（Δ +0.004629）/ soup **0.127517**（Δ +0.004161）；I1T1 raw **0.127857** / ens **0.122825**（Δ +0.005032）/ soup **0.124268**（Δ +0.003590）；individual mean/std/best/worst I0T0 0.131107/0.001880/0.129228/0.134506、I1T1 0.127253/0.002079/0.123545/0.129241；prediction disagreement mean ~0.0359/0.0366（median 0.0325/0.0315，p90 0.0558/0.0599，p95 0.0648/0.0748）；pairwise Pearson ~0.99949/0.99939、cosine ~0.99947/0.99932；soup 归一化距离 ~0.03–0.07。`confirmation_lock.json` 在**第一次读取 official valid 之前**写入（protocol hash + Top-5 manifest hashes + soup file/tensor hashes + primary metric + decision gates + timestamp）。**Stage B（官方 valid，锁定后一次性）**：**I0T0 raw 0.167234 / ens 0.163056 / soup 0.163063**；**I1T1 raw 0.157832 / ens 0.152685 / soup 0.152898**。**Δ_soup = +0.004171 / +0.004934，mean +0.004552 ≥ +0.0015（G1 两 run 均 ≥0、G2 通过）；two-run paired bootstrap（B=2000，fixed seed 20260923，n=1000）95% CI [+0.002667, +0.006456]，lower > 0（G3 通过）→ `TOP-5 WEIGHT-SOUP STABILIZATION SIGNAL`**。**Δ_ens = +0.004178 / +0.005147，mean +0.004663 ≥ +0.002** → ensemble mechanism gate 通过。**Decision Case D：`CHECKPOINT AGGREGATION STABILIZATION SUPPORTED`**（primary = weight soup，deployable form）。soup retention **rho 0.976**，ensemble-vs-soup 两 run CI 均含 0（I0T0 Δ −0.000007、I1T1 Δ −0.000214）→ **single-model soup 保留几乎全部 function-space benefit**。**cross-run point spread 未被 soup 降低**（S_raw 0.009402、S_soup 0.010165、S_ens 0.010371；Δ +0.000762，仅 secondary descriptive，不作方差估计）。**compute**：raw 18.52 ms / soup 18.74 ms（~1×）/ ensemble 95.45 ms（**5.15×**，存 410,575 params，5 个模型）。**12/12 Stage-0 integrity gates + 20 tests 全绿；official test 从未加载**。措辞限制：最多只能说「A fixed Top-5 weight average reduces the observed sensitivity of the selected estimator under the locked development protocol」，**不得称 selection noise 已解决**（只有两条 trajectories；official valid 是 locked development confirmation 而非项目级 pristine holdout；机制未必是 prediction ensembling）。**下一步授权：`checkpoint_weight_averaging_protocol_design`（仅设计，不授权 EMA/SWA，不授权 official test）**。
106. ~~raw-graph -> patch-system sufficiency audit (09-26)~~ → `notes/raw_graph_patch_sufficiency_audit.md` + `results/raw_graph_patch_system_sufficiency/`（audit_protocol_lock + raw_graph_inventory + pre_neural_input_inventory + split_inventory + hard_signature_lock + p0/p1/p2/p3_signature_summary + raw_graph_isomorphism_checks + functional_collision_checks + hard_aliasing_target_summary + hard_aliasing_lower_bounds + raw_wl_lock + raw_sp_lock + patch_soft_signature_lock + normalization_stats + distance_scale_stats + 12 个 neighbour manifest JSONL + neighbor_manifest_hashes + phaseU_integrity + target_metric_lock + random_neighbor_baseline + soft_geometry_metrics.csv + soft_geometry_bootstrap + collision_consistency + knn_predictor_diagnostics + integrity_tests + final_decision + top1_pre_neural_hypothesis + answers_q1_q20 + figure_data + 3 图）+ `records/claims/claim-raw-graph-patch-system-sufficiency-caseD-20260926.yaml` + `records/decisions/decision-raw-graph-patch-system-sufficiency-caseD-20260926.yaml`：**zero-full-training / representation-family 审计**，只研究 `G_raw -> F_pre(G)` 边界（不改 architecture / 不加 module / 不训练 / 不 EMA·SWA·soup / 不调 checkpoint / official valid 与 official test 均从未加载）。从真实代码重建 raw graph（PyG ZINC `data.x`=atomic number、`data.edge_attr`=bond type）与 compact-v4 全部 deterministic pre-neural 输入（typed/parent token、146D patch shell descriptor、23D pair relation + distance bucket + pair incidence、62D global、25D topology）。**Phase H（hard aliasing，全 10K official train，target-free 直到 hash-lock）**：progressive canonical signatures **P0** token 多重集 / **P1** full patch-object system / **P2** patch+relation system / **P3** +global+topology；canonicalization = coloured-incidence `pynauty.certificate` + canonical semantic colour sequence（exact coloured-graph invariant；node-relabel invariance 与 VF2 精确校验通过）。**P0** 5 个非单例类（mass 0.00100）、其中 **2 个 raw-non-isomorphic**（{941,2264}、{7805,9382}）、`LB(P0)=7.95e-5`；**P1 用连续 146D shell descriptor 完全消解 token alias**，P1=P2=P3 只剩 **3 个 raw-isomorphic duplicate 类**（目标完全相同）→ **`LB(P3)=0.0 < 0.002` material gate**；**P3 raw-non-isomorphic collision = 0**（functional check vacuous）。**Phase S（soft structural locality）**：frozen official-train `7200 reference / 2000 primary / 800 replication` split；raw references = edge-aware typed **1-WL rounds 0..4**（锁定不扫）与 typed shortest-path histogram；patch blocks B1 identity / B2 217D patch-descriptor projected-quantile sketch / B3 217D pair-relation sketch / B4 z-scored 87D global+topology；per-block random-pair median scaling；k=8；**所有 neighbour manifests 在 target 读取前 SHA-256 lock**。**probe η（越低越 target-local）**：RAW **0.6337**、WL 0.7455、SP 0.6024、PATCH_LOCAL 0.5865、PATCH_PAIR 0.5888、**PATCH_FULL 0.5459**；**`Delta_pre = η(PATCH_FULL) − η(RAW) = −0.0878`（paired bootstrap B=2000 95% CI [−0.0999,−0.0748]）**；800 replication −0.0909；两个 generic raw view 同方向（patch_minus_wl −0.1996、patch_minus_sp −0.0565，`raw_reference_unstable=false`）；collision rate RAW 0.0846 → PATCH_FULL 0.0464（`metric_inconsistent=false`）；NN8 median MAE 1.002→0.878（supportive）。**判定：Decision Case D — CURRENT_PRE_NEURAL_PATCH_SYSTEM_SUFFICIENCY_NOT_REFUTED**：hard 与 soft 均未显示 pre-neural bottleneck，`top1_pre_neural_hypothesis.json` `authorized_for_design=false`、`full_training_authorized=false`。**正式停止「哪里丢了信息？」这条线**；下一步唯一科学上正当的阶段是 **Inductive-Bias & Sample-Efficiency Audit**（同样信息为何需要更多样本），不是再加 structural feature/module。**progressive 解释**：PATCH_LOCAL 已优于 RAW、pair block 中性、global/topology 提供主要增益 → 不得因 PATCH_LOCAL 单独较弱而 redesign local patch。18 项 integrity tests（含 canonical relabel invariance、P3 VF2、100/100 controlled perturbation completeness）+ 20 pytest 全绿。

107. ~~compact-v4 inductive-bias & sample-efficiency audit (09-12)~~ → `notes/inductive_bias_sample_efficiency_audit.md` + `results/inductive_bias_sample_efficiency_audit/`（audit_protocol_lock + architecture_lock + split_inventory + sample_efficiency_subset_lock + anchor_protocol_compatibility + seed_lock + decision_thresholds + integrity_gates + stage1/2/3/4 JSON + decisions + learning_curve_summary.csv + three_point_learning_curve.csv + two_seed_learning_curve.csv + doubling_gain_summary + paired_bootstrap + compute_audit + final_decision + answers_q1_q20 + curves/ 4 + snapshots/ + states/ soup + 4 图）+ `records/claims/claim-inductive-bias-sample-efficiency-audit-20260912.yaml` + `records/decisions/decision-inductive-bias-sample-efficiency-audit-caseA-20260912.yaml`：**staged compute-matched learning-curve audit**（不是新 architecture / 新 structural feature / representation collision / optimizer·regularization·EMA·SWA·HPO / external-model comparison / official-test benchmark）。**唯一问题**：在 architecture、optimizer、selection set、probe set 与 optimizer-step budget 固定时，减少 unique training examples 会损失多少 held-out MAE。**环境 index-for-index 复用** frozen official-train `7200/800/2000` split（不重切；official valid 与 official test 均从未加载）。**nested target-independent subsets** `D1800 ⊂ D3600 ⊂ D7200`，排序键 `sha256(salt || original_official_train_index)`，`target_used=false`；`dataset_hash_3600=3be9f7e1…`、`dataset_hash_1800=17eb66c1…`。**compute-matched step protocol 由真实 7200 anchor 机械推导**：`steps_per_epoch=57`、`max_optimizer_steps=240×57=13,680`、`selection_eval_interval=57`、`patience=40 selection evaluations`（tie = earlier step）；`anchor_protocol_compatibility.json equivalent_at_7200=true`（optimizer/lr/wd/batch/loss/scheduler/clip/cadence/patience 逐字段一致）；**7200 I0T0/I1T1 anchors 直接复用、从未重训**；RAW = 最低 800-MAE checkpoint；SOUP = K=5 等权参数平均（固定 measurement channel，不扫 K、不做 EMA/SWA/greedy/weighted）。**初始化匹配**：四张新 run 的初始 state hash 与历史 smallhead I0/I1 fingerprint 精确一致（max_abs_diff=0）。**结果（SOUP probe MAE，2000 internal probe）**：**N1800 seed0 0.225184 / seed1 0.240017；N3600 seed0 0.176633 / seed1 0.175501；N7200 seed0 0.127517 / seed1 0.124268**（RAW 同方向：0.226539/0.244358/0.181330/0.176472/0.131679/0.127857）。**doubling gain `G_soup=E(N)−E(2N)`**：3600→7200 seed0 **+0.049116**（paired B=2000 95% CI [+0.042053,+0.056636]）/ seed1 **+0.051234**（CI [+0.042389,+0.061006]），two-seed mean **+0.050175**（CI [+0.043256,+0.057999]）；1800→3600 seed0 **+0.048551**（CI [+0.041398,+0.055593]）/ seed1 **+0.064516**（CI [+0.056034,+0.072706]），two-seed mean **+0.056533**；**平均每 doubling +0.053354**，在 1800–7200 近似常数（descriptive slope b=−0.053354 MAE per log2 N），曲线近似 log2(N) 线性。四张新 run 均 canonical patience early-stop（3600 seed0 6,042 steps/106 evals；seed1 9,063/159；1800 seed0 6,555/115；seed1 7,581/133），**无 boundary-pinned**。**train/probe gap 随 N 缩小而增大**（SOUP：3600 0.128/0.126；1800 0.187/0.199；7200 ~0.075）。**18/18 integrity gates 通过；official valid 从未加载；official test 从未加载**。**判定 Case A：STRONG_WITHIN_FAMILY_DATA_LIMITED_REGIME** → `top1_sample_efficiency_hypothesis.json` `authorized_for_design=true`、`hypothesis_family=sample_efficiency_inductive_bias`、`full_training_authorized=false`。**不**授权任意新 architecture 训练；**不**跑 N=900；**不**加 seed2/seed3；**不**做 power-law 外推；**不**称「更多数据一定追平 ~0.02 gap」；此前所有 representation NO-GO 继续有效。下一阶段只允许 **sample-efficiency-oriented inductive-bias design**（“为什么已有 structural information 能用更少样本学会”）。
108. ~~sample-efficiency gain localization audit (10-06)~~ → `notes/sample_efficiency_gain_localization_audit.md` + `results/sample_efficiency_gain_localization/`（audit_protocol_lock + model_inventory + subset_inventory + prediction_provenance + probe_predictions_locked.parquet + recomputed_learning_curve + bootstrap_provenance_check + support_table_locked.parquet + support_table_lock + support_quartile_manifest + well_covered_subset_manifest + token/relation document-frequency summaries + relation_key_inventory + size_control_lock + patchfull_metric_inventory + phaseU_integrity + gain_table.parquet + gain_summary + token/relation/relation_parent/neighbor_density family results + size_adjusted_results + fixed_ols_oof_results + support_family_correlation.csv + support_increment_diagnostics + well_covered_stress_test + raw_estimator_robustness + initial_error_control + mechanism_decision + top1_sample_efficiency_mechanism + answers_q1_q24 + final_decision + integrity_tests + figures/ 4）+ `tracks/ksvd/tests/test_sample_efficiency_gain_localization_audit.py`（17 pass）：**零新训练 target-free support → gain localization audit**。Phase U（target-free support，hash-locked，target_used=false / prediction_used=false）→ Phase Y（gain association）硬 firewall；reuse exactly six frozen SOUP states（1800/3600/7200 × seed0/1）+ RAW anchors；official valid 从未加载；official test 从未加载。Support = unique-molecule document frequency；primary token = historical `typed_certificate`（6784 unique）；primary relation key = canonical unordered typed-endpoint pair + exact integer distance + adjacent bond（655202 unique，mode A，naturally discrete）；secondary non-authoritative parent-endpoint key（2069 unique）；NN8 = frozen PATCH_FULL（7200-frozen block scales，never refit per N）。Gains G_soup 18→36 **+0.056533** / 36→72 **+0.050175**（与 prior learning curve bit-exact；all six anchor MAE reproduced；bootstrap provenance 精确复核：B=2000 seed 20260912 复现 prior point+CIs 到 <1e-12）。**No family passes full strong gate**（G2 Spearman ≥ +0.15 全部失败）：**token** quartile contrast +0.0276/+0.0336（CI>0）且 size-adjusted β1 +0.0126/+0.0159（CI>0）但 ρ≈0.061 → **NON-ROBUST ASSOCIATION**；**neighbor_density** quartile +0.0282/+0.0379 但 size-adjusted β CI 跨 0 → **SIZE/WEAK**；**relation** 仅 36→72 通过 → **REGIME-SPECIFIC/PARTIAL**。Primary WELL_COVERED（typed relation key）**n=1 → UNPOWERED**（Case E 不可 claim）；secondary coarse-key WELL_COVERED n=361 powered，R_covered 0.722/0.603 → 仅 **Case E candidate**（authoritative=false）。Fixed 5-fold OLS OOF R² ≤ +0.004（full negative）。RAW direction-consistent。**判定 Case H：SAMPLE-EFFICIENCY GAIN LOCALIZATION INCONCLUSIVE** → `authorized_for_design=false`、`full_training_authorized=false`。20/20 integrity tests pass。**不**授权任何 architecture 训练；**不**放宽 primary unseen 定义；**不**把 secondary key 当 authoritative；此前所有 NO-GO 继续有效；official valid/test 保持 locked。Revisit_if：更大 probe 或预先登记的更粗 primary relation key 使 primary WELL_COVERED stress test powered（n≥200）。

109. ~~compact-v4 shared-basis compositional interaction (SBCI) minimal falsification (10-07)~~ → `notes/compact_v4_sbci_sample_efficiency.md` + `results/compact_v4_sbci/`（architecture_lock + baseline_inventory + sbci_parameter_ledger + relation_input_inventory + initialization_lock + integrity_gates + branch_alive_tests + compute_audit + stage_run_N3600_I0T0 + checkpoint_manifest_N3600_I0T0 + soup_construction_N3600_I0T0 + stage1_N3600_I0T0 + stage1_bootstrap + stage1_decision + representation_diagnostics + final_decision + answers_q1_q20 + curves/N3600_I0T0.csv + snapshots/ + states/soup + 3 图）+ `records/claims/claim-compact-v4-sbci-nogo-20261007.yaml` + `records/decisions/decision-compact-v4-sbci-nogo-20261007.yaml`：**sample-efficiency-oriented architecture falsification**（不是新 feature / richer representation / bigger model / message passing / attention / pooling / tokenizer / radius / checkpoint-tuning / HPO）。唯一假说：把 compact-v4 自由的 relation/centre latent function family 替换为**共享低维 functional basis + factorized relational composition** 能提高有限数据 sample efficiency。**Hard principle = replace, do not append**：保留 historical rooted tokenization / token embedding / parent embedding / patch construction / shell·path·topology descriptors / patch encoder / complete patch-pair enumeration / 5 distance buckets / 23D deterministic relation descriptor / global branch / topology branch / loss / optimizer / data protocol；**不 instantiate** original pair projection（768）、relation encoder（1,360）、distance gate（80）、pair encoder（5,328）、213D centre-update MLP（15,888）、original 302D R、original 302→13→13→1 head（4,135）——no residual bypass。SBCI：`K=16` fixed（no sweep）；`z_i=phi(h_i)`（48→32→16，no LN/dropout/residual/output activation）；relation-only `g_ij=1+tanh(psi(r_ij))`（23→32→16，**endpoint learned state 不进入 psi**）；`p_ij=z_i*z_j*g_ij`（唯一 pair 表示，无 concat/bilinear/pair MLP/additive branch）；5 buckets × [mean 16 ; population std 16 ; log1p(count) 1] = **165D**；`delta_i=W_c A_i`（**single Linear(165,16)**，无 hidden/activation/LN）→ `c_i=z_i+delta_i`；graph mean/population std/sqrt-normalized sum → 48D；`R_SBCI=[C(48);G(32);T(8)]=88D`；head `88→16→1`。**参数 62,045 ≤ 82,115 cap（Δ −20,070；不凑参数）**。**Stage 0 全过 25/25**：baseline 82,115；shared init **20 tensors max_abs_diff 0.0 + SHA-256 match**（canonical baseline 先 instantiate、SBCI-only 用 sub-seed `sha256("SBCI-v1|seed")`、再 exact copy shared），F0.1/F0.2 retained encoder outputs bit-identical，pair factorization/endpoint symmetry/empty-bucket zeros/permutation invariance（diff 0.0）/R=88D/finite outputs 全部精确；branch-alive：phi_grad 0.263、psi_grad 0.00719、centre_grad 0.745、head_grad 0.740；zero-pair pred shift 0.00796、zero-basis pred shift 0.0865。**Stage 1（唯一 ticket，N3600 seed0，compute-matched：max 13,680 steps / eval 57 / patience 40 / L1 / Adam lr 1e-3 wd 1e-5 / batch 128 / K=5 soup）**：best 800-select **0.215511 @ step 3534**（5,814 steps / 102 evals / 200.5 effective epochs；early-stop，non-boundary-pinned，无 NaN）；**SOUP probe 0.182924 / RAW 0.187545 vs baseline 0.176633 / 0.181330** → **`delta_SE,soup = −0.006291`、`delta_SE,raw = −0.006215`、`DDR = −0.128`**；paired bootstrap（B=2000 seed 20261007）SOUP 95% CI **[−0.012760, −0.000176]**（candidate 显著更差），RAW CI [−0.013078, +0.000696]。**判定 Case A：`SBCI_MINIMAL_FACTORIZATION_NO_GO`**（`delta_SE < +0.008` 的 clean NO-GO，且方向为负）。**seed1 未购买；N7200 / N1800 / seed2 / seed3 未运行；无 rescue（不扫 K、不加 residual q/centre、不加 attention、不加宽）；official valid 与 official test 均从未加载**。compute 单独报告：62,045 vs 82,115 params；128-mol forward **7.66 ms vs 19.43 ms（−60.6%）**，peak RSS ~2.0 GB（pair encoder 与 213D centre update 被删除）。与 P1（保留 q 加 learned composer，NO-GO）/P2（recompute q，NO-GO）/cell（persistent cycle cells，NO-GO）机制不同：SBCI **删除** free relation/centre function family；与旧 compositional patch sharing oracle（frozen 推理替换 rare/OOV exact token embedding，保留全部 q/centre/302D R/large head，0/4 seed 退化）不同：SBCI 是 from-scratch function-class restriction，不是 embedding transplant。正确措辞：这只关闭**这一最小 shared-basis factorization**，不否定 compositional inductive bias 一般命题。`tests/test_compact_v4_sbci.py` 12 tests 全绿。

110. ~~SBCI fit-vs-generalization failure triage (10-07)~~ → `notes/sbci_fit_generalization_triage.md` + `results/sbci_fit_generalization_triage/`（audit_protocol_lock + baseline_run_inventory + sbci_run_inventory + triage_protocol_compatibility + train_eval_raw + train_eval_soup + train_abs_err_{raw,soup}_{baseline,sbci} + snapshot_train_fit.csv + snapshot_train_fit.summary + best_train_fit + late_trajectory_fit + convergence_diagnostic + generalization_gap + parameter_utilization + triage_decision + next_architecture_family + answers_q1_q16 + final_decision）+ `records/claims/claim-sbci-fit-generalization-triage-inconclusive-20261007.yaml` + `records/decisions/decision-sbci-fit-generalization-triage-inconclusive-20261007.yaml` + `tracks/ksvd/tests/test_sbci_fit_generalization_triage.py`（18 pass）：**zero-training architecture-family triage**（不是新 architecture / SBCI rescue / K·width sweep / optimization search / new seed / new split / representation audit / HPO）。**唯一问题**：SBCI 失败是因为 function class 过度约束到连训练函数都拟合不好（APPROXIMATION / OVERCONSTRAINT），还是拟合足够但 inductive bias 更差（WRONG BIAS DESPITE ADEQUATE FIT）？前者授权 **SSOD**，后者授权 **DSP**，mixed 则都不授权。**只用已存在的 N3600/I0/T0 compact-v4-smallhead 与 compact-v4-SBCI trajectories/checkpoints；零 gradient update（无 `.backward()` / `optimizer.step`）；official valid 与 official test 均从未加载**。**protocol comparability = true，16/16 checks**：同一 run_id/N=3600/I=0/T=0、同一 D3600 indices（`sha256 3be9f7e1…`）、同一 800/2000 index hashes、同一 subset salt；optimizer Adam / lr 1e-3 / wd 1e-5 / batch 128 / L1 / clip 5.0 / max 13,680 steps / eval 57 / patience 40 / K=5 等权 soup 逐字段一致；两边重建 init hash 与 frozen manifest 精确一致（baseline `63f2cecb…`、SBCI `f3479439…`），**20 个 shared tensors `max_abs_diff=0.0`**。**D3600 deterministic `model.eval()` full-training-set fit**：RAW baseline **0.073954**（step 3,762）vs SBCI **0.072016**（step 3,534）→ **`dF_raw = −0.001938`**（paired B=2000 95% CI [−0.004562,+0.000603]，跨 0）；SOUP baseline **0.048170** vs SBCI **0.052724** → **`dF_soup = +0.004554`**（CI [+0.002693,+0.006696]，排除 0）——**RAW 与 SOUP 符号冲突**。**best-achieved `F_min`（208 个 snapshot 全部 eval-mode 重算，与 stored curve max|diff|=0.0）**：baseline **0.055960**（step 5,985）vs SBCI **0.059002**（step 5,301）→ **`dF_min = +0.003042`**；**late-10 median**：0.069631 vs 0.068679 → **`dF_late = −0.000952`**。**判定 Case C：`FIT_GENERALIZATION_TRIAGE_INCONCLUSIVE`**：Case A 失败（无 +0.008 fit loss，最大只有 +0.0046），Case B 失败（`dF_soup=+0.00455 > +0.003`，`|dF_min|=0.003042 > 0.003`，且 RAW/late 反向）→ **`authorized_for_design=false`、`family=null`、`full_training_authorized=false`，不授权 SSOD、不授权 DSP、不 rescue SBCI**。**Convergence**：literal −5e-5 per-eval threshold 被 cross（SBCI slope −0.002014）但 **统计不显著**（SE 0.003259、t −0.618、bootstrap 95% CI [−0.008627,+0.003827]），且 SBCI best-so-far 在最后 10 evals 只改善 **+0.000993**（baseline +0.008651）→ 已 plateau；`convergence_warning=false`，严格 literal 读法下 case 会是 `OPTIMIZATION_AMBIGUOUS`（同样不授权 architecture），两者都在 artifact 中显式记录。**probe degradation 作为输入**：+0.006291（RAW +0.006215）；`delta_gap = +0.001737`（仅描述）。**不重新判断 SBCI NO-GO**；`F_min` 仅为 diagnosis，绝不参与 model selection；无可部署新 checkpoint。Revisit_if：更大 fit 测量（>1 seed/N pair）或预先登记的 noise-robust convergence statistic 能解决 RAW-vs-SOUP 符号冲突。

111. ~~SBCI fit-matched generalization frontier (09-12)~~ → `notes/sbci_fit_matched_generalization_frontier.md` + `results/sbci_fit_matched_generalization_frontier/`（audit_protocol_lock + protocol_compatibility + snapshot_inventory + train_fit_inventory.parquet + fit_inventory.summary + train_fit_overlap + fit_anchor_lock + matched_snapshot_pairs.csv + phaseF_fit_only_lock + match_quality + stage1_800_per_anchor.csv + stage1_800_per_molecule.npy + stage1_800_bootstrap + stage1_800_decision + endpoint_context + final_decision + next_architecture_family + answers_q1_q20 + integrity_tests + 3 图）+ `records/claims/claim-sbci-fit-matched-generalization-frontier-crossing-20260912.yaml` + `records/decisions/decision-sbci-fit-matched-generalization-frontier-caseC-20260912.yaml` + `tracks/ksvd/tests/test_sbci_fit_matched_generalization_frontier.py`（29 pass）：**SBCI branch 的最终 zero-training 诊断（不是新 architecture / SBCI rescue / optimization continuation / K·width sweep / checkpoint-selection / soup construction / HPO）**。**唯一问题**：在相同 empirical D3600 training fit 下，SBCI 是否系统性地比 `compact-v4-smallhead` 泛化更差？**Hard rule = zero gradient updates**（runtime guard 禁止 `.backward()` 与 `optimizer.step()`）；**只读已存在的 N3600/I0/T0 两条 RAW trajectory snapshots**（baseline 106、SBCI 102）；**official valid 与 official test 均从未加载**。**Phase F（fit only）**：对全部 **208** 个 snapshot 在 `model.eval()` 下重算 D3600 train MAE（max|recomputed−stored| = **0.0**）；**protocol compatibility 16/16**（同 run_id/N/I/T、同 D3600 `3be9f7e1…`、同 800/2000 index hashes、同 salt、同 optimizer recipe、同 13,680 step budget、同 57-step cadence、同 patience 40、同 I0/T0、20 shared tensors max_abs_diff 0.0）；25% burn-in（`S_common = 5814`，threshold **1453.5**，eligible baseline **81** / SBCI **77**）；common train-fit overlap **[0.0590017, 0.1680832]**；10% trim → **[0.0699098, 0.1571750]**（width 0.0872652）；**K=7** equal-spaced anchors；deterministic minimum-cost bipartite matching（cost = |train MAE − anchor|，later-step tie-break ε=1e-12）；**K_valid = 6**（anchor 6 invalid：baseline proximity 0.00777 > 0.002）；median cross-model fit gap **0.0008688 ≤ 0.001** → `fit_anchor_lock.json`（lock_sha256 `85fbd6cc…`）与 `matched_snapshot_pairs.csv`（`44d6a246…`）在任何 800 访问前 **SHA-256 lock**。**Phase G800（selection-generalization diagnostic set，不是 pristine holdout）**：只对 6 个 valid locked pairs 求值；equal-weight anchors（先对 molecules 平均、再对 anchors 平均）：`D_k = [+0.006618, −0.003695, +0.008842, +0.021548, −0.003175, −0.010575]`；**`D_mean = +0.003261`**；paired-molecule bootstrap（B=2000 seed 20260912）95% CI **[−0.004861, +0.012669]**；median D_k **+0.001722**；`f_+ = 0.50`；`f_{≤0.002} = 0.50`。**Material frontier crossing fires**：**3** anchors `D_k ≥ +0.003` 且 **3** anchors `D_k ≤ −0.003`。**判定 `MATERIAL_FRONTIER_CROSSING` → 直接 INCONCLUSIVE；`authorize_2000 = false`**。**Phase G2000 未打开**（`stage2_2000_*` 不存在；无 placeholder）；严格服从 §31/§42 的 stop rule。**最终 Case C：`FIT_DEPENDENT_FRONTIER_CROSSING`** —— matched-fit 下 SBCI 的相对泛化**材料性地依赖 fit level**，不存在稳定 ordering，因此 **不授权 SSOD、不授权 DSP、不授权 SBCI rescue、不再做第四个 SBCI diagnostic（禁止 fit-bin 细分 / 5·9·11 anchors / tolerance-threshold sweep / subgroup / seed1 SBCI / K rescue）**；`authorized_for_design=false`、`family=null`、`full_training_authorized=false`、`sbci_branch_closed_for_architecture_inference=true`。**不重新判断 SBCI endpoint NO-GO**；SOUP 仅作 contextual endpoint markers（baseline SOUP train 0.048170 / 800 0.208414 / 2000 0.176633；SBCI SOUP train 0.052724 / 800 0.214427 / 2000 0.182924），绝不进入 frontier decision。**20/20 integrity tests + 29 pytest 全绿**。注意：早期 SBCI records 的 repository date 为 20261007，本 audit runtime date 为 **2026-09-12**，record metadata 中显式记录 `runtime_date` / `repository_record_date` / `date_policy` / `chronology_note` 以避免 chronology 歧义。下一步：SBCI-derived architecture inference 结束；未来 architecture idea 必须来自**独立原则**，而不是对 SBCI 失败的 post-hoc 解释。
