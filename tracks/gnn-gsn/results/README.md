# results/

本轨**无结果**：历史 `results/REGISTRY.md` / SCHEMA 随旧仓 `../../paper` 遗失（见 TRACK.md）。

现行结果 → `tracks/gsn-replication/results/`（git-ignored；协议 `gsn-strict-social-v1`）。

声明：新旧结果不可混表——历史数字不存在，且 `strict-v0`（旧）与 `gsn-strict-social-v1`（新）协议口径不同。
