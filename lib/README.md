# lib/

跨轨可复用、**无课题叙事** 的代码。

| 子目录 | 用途 |
|--------|------|
| `data/` | 数据集加载、名称别名 |
| `eval/` | fold / protocol / result schema |
| `report/` | 聚合 registry、导出表 |

当前为空：有第二轨真正复用时再从 track `code/` 上抽，避免过早抽象。
