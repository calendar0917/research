1. 衡量 KSVD 对矩阵重建的指标应该如何设计？
- 如果单单用重建误差，可能无法衡量对图表征的优劣？因为重建误差针对的是字典对多个 patch 的还原度量，而不是对图的度量

2. 邻接矩阵的置换不变性问题
- 一方面是统计式的 patch，包括节点、的特征信息
- 一方面是采用多种排序指标，不单单是度排序(?)

3. 不一定是随机游走，只是一个解决问题的思路

4. 当前采样到的 patch 只是片段，但是片段之间的内在关联问题没有解决。
