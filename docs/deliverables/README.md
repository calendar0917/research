# 交付物

对外材料放这里（导师一页、答辩提纲等）。

约定：

- **覆盖写**当前版：`当前_*.md` / `当前_*.xlsx`
- 需要留档时用子目录：`YYYYMMDD/`
- 禁止在 `docs/experiments/` 按日堆一堆 `mentor_brief_日期.md`（旧仓习惯不再延续）

GNN 阶段已有导师表：原在 `../../paper/docs/experiments/`（旧仓 2026-09 核验已不可访问，该表未迁入本仓、现已遗失）。如需交付表，需基于现行轨重建。
